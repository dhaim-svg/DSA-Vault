# Sprint 025 Handoff — Buch-Sternchen (B-023), Druck des Zauber-Tabs (D-052), Geld-Tests (B-025)

## Fertig (alle Tasks abgeschlossen)

- ✅ T0: Sprint scaffold (`BACKLOG.md` D-052 → In Progress, Vault-`backlog.md` B-023/B-025 → In Progress, `plan.md`) — `cf2206b`; Plan-Korrektur nach Prototyp (Platzhalter statt Inline-Regel, Rulings R2–R4) — `f23b579`
- ✅ T1: **B-023** — `parsers/wikiartikel.py::render_markdown` schützt die Buchnotation `ZfP*`/`LkP*`/`RkP*`/`TaP*` vor mistune (Platzhalter U+E000 hin und zurück, Lookahead `(?!\*)` schützt `**LkP**`); alle Artikelvorschauen (SF, Zauber, Ritual) laufen über die eine Aufrufstelle `_load_one`; B-020-Tests auf `render_markdown` umgestellt; Korpus-Tests (Stern-Bilanz **82 → 0 Dateien**, kein U+E000 im Wiki); Static-Render: 2 Zeilen (Lichtblitz, Antimagie); +18 Tests — `3f9fce1`
- ✅ T2: **D-052 Vermessung** (Browser, nur lesend, vor jedem Eingriff) — der Überlauf kam aus dem `.spell`-Grid der Zauberliste, nicht aus „Spontane Modifikationen“; 20 Kontrast-Gruppen statt der 3 bekannten Werte (Rulings R6–R9) — kein Commit
- ✅ T3: **D-052 Fix** — `tabs.css` (nur `@media print`): 20 Gruppen `paper-ink` (vier Selektoren mit `#tab-zauber`-Präfix), Vol-Badge/Speicherkasten/Namenslink-Unterstrich lesbar, Slot-Schaltflächen im Druck aus, Druck-Grid mit `minmax(0,Xfr)`-Spalten (überlaufsicher); +6 Tests — `178bf37`
- ✅ T4: **B-025** — Geld-Unittests prüfen `load_held` statt der Replik `_make_geld_dict` (gestrichen); `else`-Zweig und `safe_int` erstmals getestet; +5 Tests — `5e38ed4`
- ✅ T5: **Browser-Nachmessung** (nur lesend, Print-Emulation 794/718/615 px + Bildschirm 1280/400 px) — BESTANDEN: Überlauf 881 → 779/703/600 px, 0 Kontrast-Restgruppen, 400 Bildschirmwerte 0 Abweichungen — kein Commit
- ✅ Polish (Controller inline, Final-Review M1): Korpus-Test erkennt ein Platzhalter-Leck in Link-URLs — `076421b`
- ✅ T6: `verification.md`, `plan.md` (Task-Stände, Out of Scope, Rulings R1–R9), Vault-`backlog.md` (B-023/B-025 → Done, B-026 neu), `BACKLOG.md` (D-052 → Done mit korrigierten Zahlen, D-053 neu); Wrap (dieses Dokument, `CLAUDE.md` → 25)
- Commits liegen **lokal** auf `master`: **nichts von Sprint 022 (ab T2), 023, 024 und 025 ist gepusht** (44 Commits vor `origin/master` vor dem Wrap-Commit).

## Was funktioniert

- **Artikelvorschau (SF, Zauber, Ritual):** Buchsterne (`ZfP*`, `LkP*`, `RkP*`, `TaP*`) stehen literal im Text; umschließende Kursivspannen (`*…TaP*-Schwellen…*`) und `**LkP**` bleiben korrekt; im Static-Render sind die 2 falsch gepaarten `<em>` weg (`<em>` 16 → 14).
- **Druck des Zauber-Tabs (Print-Emulation, alle 8 Tabs sichtbar):** Kopfzeile und 6-Spalten-Zauberliste passen in die Seitenbreite (703 px = A4 abzüglich Ränder, auch 600 px überlauffrei), alle Texte 14,62 : 1, Vol-Badge und Zauberspeicher als Kästen mit sichtbarem Rand, „+ Befüllen“ und die Slot-Bedienelemente erscheinen nicht im Druck, offene Artikel drucken lesbar und dürfen umbrechen. **Bildschirm unverändert** (Golden-Diff = reine Einfügung von 27 Zeilen im Druckblock + die 2 `<em>`-Zeilen).
- **Tests:** `test_inventar_model.py` prüft den echten Geld-Parser (Struktur, Kurs 1234, 5 Fallback-Fälle, krumme Werte); `test_wikiartikel.py` sichert die Buch-Sterne am ganzen Wiki-Korpus (~3 s).
- Alle Features aus Sprint 013–024 unverändert (Chronik Roh/Kompiliert/Register + Suche + Druck-Kopfzeile, Eigenschafts-Leiste, Inline-Werte, Zauberspeicher, Aussehen, Würfel, Steigern, Inventar, Sprachen, ZfW-Sortierung, Wund-Malus + Hausregel-Chips, Touch-Ziele @ 400 px, SF-/Zauber-/Ritual-Artikelvorschau, Desktop-Footer über dem Würfelpanel, Static-Render mit eingebettetem JS).

## Verifikation

- **Test Suite:** **615/615** bestanden, auch mit `-W error` (pytest-Option), beide Läufe nach Löschen aller `__pycache__` (Sprint-024-Stand 586: +18 T1, +6 T3, +5 T4). Mutationsproben je neuem Assert-Block (Reports unter `.superpowers/sdd/sprint-025/`, git-ignoriert).
- **Static Render:** `output/illaen-baernhold-dashboard.html` erfolgreich generiert (exit 0), 502 929 B LF-normalisiert (511 374 B CRLF im Working Tree); 50× `class="artikel-details`, 0× `<script src`; `git status` nach dem Render sauber.
- **Wiki:** `check-frontmatter.py` → 828 Dateien, 682 mit Frontmatter, 0 fehlerhaft; `git diff --numstat faf478b..HEAD -- wiki helden/illaen-baernhold abenteuer` **leer** (weder Wiki noch User-Domäne angefasst).
- **Reviews:** Task-Reviews T1/T3/T4 (Sonnet) je Spec ✅ / Approved (0 Critical, 0 Important); Gesamt-Review (Opus): *Ready to merge: With fixes* — 1 Important (Scope-Grenze von D-052 in den Dokumenten unsichtbar, **behoben**), 7 Minor (M1 behoben, Rest deferred); Rulings R2/R6/R7/R9 bestätigt. Berichte: `.superpowers/sdd/sprint-025/`.
- **Browser-Runden:** T2 (vor) und T5 (nach dem Fix), Static über `http.server`; echter Druckdialog und echter `PATCH`-Pfad weiter ungeprüft.

## Als nächstes (Sprint 026)

- **D-053** (Dashboard, S–M, ready) — **Druck-Kontrast-Sweep der übrigen 7 Tabs.** D-052 deckt nur den Zauber-Tab; die anderen wurden nie vermessen. Bekannt: `.card-title .meta`/`.card > h4` außerhalb 1,59–1,95 : 1, AP-Zahlen der Zeile „AP & Steigerung“ im Druck fast unsichtbar. Vorgehen wie D-052: erst je Tab messen, dann im `@media print`-Block fixen; die vier `#tab-zauber`-Selektoren brauchen für andere Tabs eigene Regeln. Optional dabei (rein kosmetisch, im D-052-Umfang bewusst nicht behoben): Pfeil ↗ des Namenslinks im Druck per geschütztem Leerzeichen an den Namen binden (steht @ 703 px bei 5 von 25 Namen allein in der Folgezeile), Probe-Spalte schmaler/einzeilig.
- **B-026** (tooling, S) — `test_inventar_model.py::test_inventar_gewicht` bildet die Gewichtsschleife aus `parsers/held.py:534–540` zeichengleich im Test nach (gleiches Replik-Muster wie B-025): auf `load_held` mit Mini-Held umstellen.
- **Kandidaten für neue EPICs (noch nicht im BACKLOG):** Footer-Einblenden glätten (`transition` auf `bottom`, aus Sprint 021); echter PATCH-Pfad im Browser (gegen eine Vault-Kopie); Grid-Stretch der Ritual-/SF-Karte (aus D-052 ausgeklammert); Zustände: optionale LE-/AU-Mali (vom User zweimal abgewählt — nicht ohne neue Anfrage).
- (Reihenfolge = Empfehlung, nicht Pflicht.)

## Bekannte Einschränkungen (bewusst ausgeklammert)

- **D-052 betrifft nur den Zauber-Tab:** Die übrigen 7 Tabs wurden im Druck nie gemessen (Ruling R7) — „D-052 done“ heißt nicht „Druck ist insgesamt lesbar“ (→ D-053).
- **Druck-Kosmetik im Zauber-Tab:** Die Probe bricht in 25/25 Zeilen zweizeilig um (Zeilen dadurch höher); der Pfeil ↗ steht bei 5 von 25 Namen (Abvenenum, Custodosigil, Fulminictus, Gardianum, Ignifaxius) @ 703 px allein in der Folgezeile (@ 794 px: 1). Bei 615 px (unter der echten Breite) trennen 3 Kosten-Werte mitten im Wort. Alles lesbar, nichts überläuft.
- **Emulation ≠ echter Druck:** Die Print-Emulation kennt `@page`-Ränder nicht; die echte Seitenbreite (≈ 703 px) wurde über einen 718-px-Viewport nachgestellt. Ein echter Druckdialog/PDF ist nie geprüft (unverändert aus Sprint 020/021).
- **Gefüllte Zauberspeicher-Slots im Druck ungemessen** (im Render alle 3 leer): `.slot-mods-row`, `.slot-mods-label`, `.slot-erneuerung-label`, `.speicher-slot.leer .slot-header` sind nur vorsorglich per CSS abgedeckt, `.slot-entleeren-btn`/`.slot-ausloesen-btn` im Druck ausgeblendet.
- **Der lange Ritual-Artikel „Zauberspeicher“** läuft im 2-Spalten-Druckfluss der Ritualliste in die rechte Spalte (vorbestehend, nicht vom Fix verursacht).
- **B-023 — Grenzen der Vorbehandlung:** nur die vier Kürzel `ZfP`/`LkP`/`RkP`/`TaP`; ein Buchstern in einer **Link-URL** (Markdown-Link, Autolink, Referenzdefinition) bliebe als `%EE%80%80` im `href` (Korpus: 0 Vorkommen, der Korpus-Test schlägt an); ein ungeschütztes `<Kürzel>***` ohne Backslash bliebe literal `***` (Korpus 0). **Andere Wort+Stern-Muster** (im Pre-flight z. B. `Probe*` ×52, `AP*` ×28) wurden **nicht untersucht** — ob sie ebenfalls falsch gepaart werden, ist offen.
- **Deferred Minors des Sprints (Testhygiene, Gesamt-Review getriagt „bleibt deferred“):** T1 — `<Kürzel>***` unmaskiert (`wikiartikel.py:32`), Link-URL-Leck-Detail (`:40`, Korpus-Test jetzt erkennt es), Test (e) `escapter Stern` ohne Mutationsbeleg (`test_wikiartikel.py:179`), Reviewer zählt 512 statt 508 Notationsdateien (ohne Einfluss); T3 — Bildschirm-Wächter (`test_rendering.py:1043–1046`) erkennt nur Lecks mit `paper-ink`/`paper-rule`/`!important`, `gap`-AttributeError statt lesbarer Assertion (`:1026`), Kommentar „vorsorglich“ zu `.speicher-slot.leer .slot-header` (`tabs.css:104`; es hat 3 Treffer), Test verlangt die Schreibweise `.spell > *` (`:1035`); T4 — kein Teil-Dict-Fall (`geld: {dukaten: 5}`), nur `dukaten` nicht-numerisch (`test_inventar_model.py:82–90`); Gesamt-Review — Korpus-Test ~3,1 s (≈ 20 % der Suite), Über-Parametrisierung ×4 (`test_buchstern_einzeln…`, `…zwei_vorkommen…`), redundante Regel `.slot-befuellen-toggle/-form` unter `-area` (`tabs.css:117`), toter Assert `fixed <= 120` (`test_rendering.py:503–504`).
- **Live-Kopplung am Bogen bleibt** bei genau zwei Tests (`test_build_context_wiki_artikel_smoke_live_vault`, `test_held_stabzauber_anchor_links_hit_a_section_of_the_wiki_article`); übrige ~14 `.index`-Vorkommen in `test_rendering.py` bewusst unverändert.
- **„Stabverlängerung“ = „Doppeltes Maß“ ist nur semantisch bestätigt** (Sprint-023-Ruling R10, unverändert); Flammenschwert und Schuppenhaut stehen nicht im Bogen (nicht eingebettet).
- **`python -W error -m pytest` scheitert in dieser Umgebung schon beim Plugin-Import** (ipykernel-Warnung) — `-W error` als pytest-Option nutzen.
- **Zeilenenden** (autocrlf=true, jede Datei behält ihr Ende; Index durchgängig LF): CRLF im Working Tree = `held.py`, `rendering.py`, `zauber.j2`, `tabs.css`, `test_rendering.py`, `test_wikiartikel.py`, `output/*.html`; LF = `test_held.py`, `test_inventar_model.py`, `heldfixtures.py`, `parsers/wikiartikel.py`, Wiki-`.md`, Tracker, `rituale.md`, neue Sprint-Dokumente. Messen per `git ls-files --eol` oder Python-Bytezählung, nicht per `grep $'\r'`. **Golden-Vergleiche:** die Baseline aus `git show <commit>:output/…` ist LF, der Working Tree CRLF — vor dem Vergleich normalisieren.
- **L24/L25 und Buchabgleiche zitieren gitignorierte Rohdateien** (`raw/pdf-extracted/wege-der-zauberei/kapitel-09-rituale.txt`); das spaltenrekonstruierte Transkript `.superpowers/sdd/sprint-023/wdz-stabzauber-transkript.txt` ist ebenfalls git-ignoriert (lokal erhalten).
- **Lehren (Process):** (a) **Messen vor Fixen und Prototyp am Korpus vor dem Plan:** T2 widerlegte die Backlog-Zuordnung des Überlaufs (Ursache `.spell`-Grid, nicht „Spontane Modifikationen“) und fand 20 statt 3 Kontrast-Gruppen; der Prototyp widerlegte die geplante mistune-Inline-Regel und die Zeilen-Metrik (Ganzdatei-Bilanz statt Zeilen). (b) **Auch der eigene Prototyp braucht eine Gegenprobe:** seine Bilanz „neu: 0“ war nur zufällig 0 (die kaputte Ersetzung von `**LkP**` glich die Zählung aus) — der Implementierer fand es über zwei rote B-020-Tests; Metrik-Definition gegen den Renderer *ohne* die Änderung prüfen (RED-Beleg: 82 Dateien). (c) **Kein `sed`/Heredoc für Backslashes** (Steuerzeichen-Falle trat erneut auf: `sed` verschluckte den Backslash in einer Regex) — Edit-/Write-Werkzeug. (d) **Tracker-Umbau mit Python nie an `---` als Abschnittsende ausrichten:** die erste `---` nach „## In Progress“ ist die Tabellen-Trennzeile; das hinterließ Rest-Zeilen im Vault-`backlog.md` (sofort per Diff gefunden und repariert) — Umbauten immer diffen. (e) **Scope-Grenzen gehören in die Dokumente, die der User liest** (`plan.md` Out of Scope, `BACKLOG.md`-Eintrag), nicht nur ins Ledger: der Gesamt-Review stufte „D-052 done klingt wie Druck insgesamt behoben“ als Important ein. (f) Reviews T1/T3/T4 und die Browser-Runden liefen ohne Fix-Runde; der Controller sah die Screenshots selbst (Sprint-024-Lehre bestätigt: der Nebenbefund der unsichtbaren AP-Zahlen stand nur im Bild).
- **Process-Notizen:**
  - Briefs/Ledger/Reports von Hand unter `.superpowers/sdd/sprint-025/` (git-ignoriert, bleibt für die Verweise in `verification.md`/`plan.md` erhalten); Reviewer-Verdikt zuoberst, < 3500 Zeichen — kein Bericht abgeschnitten.
  - Implementierer/Task-Reviews/Browser-Agent Sonnet, Gesamt-Review Opus; keine parallelen Implementierer (T5 lief parallel zum T4-Review, beides nur lesend); jeder Task-Commit per `git show --stat HEAD` gegen die erwartete Dateizahl geprüft; Controller-Gegenprobe (Testzahl + Render-Reproduzierbarkeit) nach jedem Task. Der Browser-Agent (T2/T5, ein Agent mit intaktem Kontext per `SendMessage`) hielt die Nur-Lesen-Liste ein, beide Server-PIDs beendet, `.playwright-mcp/` gelöscht. T3-Implementierer hat über den Brief hinaus selbst einmal im Browser gemessen (ohne Repo-Spuren).
  - Kein Worktree, Commits direkt auf `master` (Ruling R1, CLAUDE.md-Workflow); kein Push.
- **Aus Sprint 017–024 unverändert offen:** Register hängt am Session-Format; `Verlauf`-Speichern in der Kompiliert-Ansicht nie im Browser angeklickt; inhaltliche Sichtung der vier Sessions (47 `(?)`-Stellen); Kommando-Mängel von `/session-compile`; Static-Render-Schreibaktionen liefern unter `file://` Fake-Erfolg; Chronik-Parser-Kleinigkeiten; `.journal-readonly`-Kontrast ~3,3:1; Sprint-013-Erbe (Kampf-Header-Wortlaut, `.talent-row`-Grid, Portrait-Plumbing); Tooltips auf Touch nicht sichtbar; Steigern-Negativfall @ 400 px nur bei 1280 px und im node-Test belegt; Desktop-Footer ≤ 480 px bleibt statisch; Footer springt beim Einblenden des Panels (Sprint 021); SF-Vorschau-Randfälle (`###`-/Blockreferenz-Anker, leere Abschnitte, doppelte Anker); Zustands-Chips = Hausregel, Wund-Regeln teilweise ausgeklammert (Wundschwellen, Zonenwunden, „Wunden ignorieren“); `js_function` (tests/jsfixtures.py) findet keine Funktion mit `)` im Default-Parameter; identische lange Quelle in allen 10 Ritual-Vorschauen (Artikelspanne, kein Defekt); Ritual-Karte wächst bei vielen offenen Artikeln stark (Grid-Stretch); `_QUELLE_TEXT_RE` erkennt `> **Quelle: X**` nicht (Korpus 0 Fälle).
