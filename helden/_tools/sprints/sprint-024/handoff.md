# Sprint 024 Handoff — Ritual-Artikelvorschau (D-051) & Test-Restkopplung II (B-024)

## Fertig (alle Tasks abgeschlossen)

- ✅ T0: Sprint scaffold (`BACKLOG.md` D-051 als EPIC angelegt → In Progress, Vault-`backlog.md` B-024 → In Progress, `plan.md`) — `fd6f593`
- ✅ T1: **Apport-Namenszelle** in `helden/illaen-baernhold/rituale.md` → `[[wiki/dsa-4.1/rituale/stabzauber#Apport\|Apport]]` (einmalige User-Freigabe, genau 1 Zeile; `load_held`-JSON und Render byte-identisch) — `9b04c9d`
- ✅ T2: **Parser** liefert `wiki_path` je Stabzauber-/Ritual-Zeile (immer vorhanden, `None` ohne Link, **nur aus der Namensspalte** — die Effekt-Zelle der Apport-Zeile trägt einen eigenen Link auf `rituale-grundregeln`); zwei Sprint-023-Tests angepasst („Anker-Link ändert das Dict nicht“ gilt jetzt bis auf `wiki_path`), +6 Tests — `82a8654`
- ✅ T3: **`build_context`** lädt die 10 Ritual-Artikel (`rituale.get('stabzauber')`/`'andere'`, defensiv gegen fehlenden Schlüssel); der bewusst live gekoppelte Smoke-Test erlaubt jetzt Ritual-Pfade (R2), +5 Tests — `6fe96a6`
- ✅ T4: **Template** `zauber.j2` nach SF-Muster — Namenslink nur um den Namenstext (`.vol-badge`/`.meta` außerhalb), `<details class="artikel-details">` als direktes `li`-Kind, Zeilen ohne Link/Artikel byte-genau altes Markup; Static-Render 40 → **50** Vorschauen; +7 Tests — `209cbc9`
- ✅ T4-Fix (Fix-Runde 1, Fund der Browser-Runde): **Druck-Kontrast des Namenslinks** 1,07 → 14,62 : 1 (`tabs.css`: `.sf-list li .sf-name a` + `::after` → `paper-ink`), +1 Test — `8b522e5`
- ✅ T5: **B-024** — `test_load_held_geld_integration` synthetisch (krumme Werte, Kurs 1000/100/10/1 aus dem Parser), Kampf-Wund-Hooks Fall „mit Waffe“ synthetisch und nicht-vakuös (`_weapon_cards == 1`), `_pos()` mit lesbarer Meldung an den zwei benannten `.index`-Stellen; +4 Tests — `ed0a51a`
- ✅ T6: **Browser-Runde** (nur lesend, Static über `http.server`, Playwright-MCP; 1280/400 px/Druck-Emulation) — Layout durchweg bestanden, Druck-Kontrast → Fix-Runde, Nachmessung bestanden
- ✅ Polish (Controller inline): Kommentar zur Fixture-Vorbelegung (Final-Review-Minor) — `fd28413`
- ✅ T7: `verification.md`, `plan.md` (Task-Stände, Rulings R1–R9), Vault-`backlog.md` (B-024 → Done, B-025 neu), `BACKLOG.md` (D-052 neu) — `2ec874f`; Wrap (dieses Dokument, D-051 → Done, `CLAUDE.md` → 24)
- Commits liegen **lokal** auf `master`: **nichts von Sprint 022 (ab T2), 023 und 024 ist gepusht** (33 Commits vor `origin/master` vor dem Wrap-Commit).

## Was funktioniert

- **Zauber-Tab, Karte „Stabzauber & Rituale“:** die 9 Stabzauber-Zeilen und die Apport-Zeile haben je eine aufklappbare Artikelvorschau (`▸ Artikel`) mit dem passenden `##`-Abschnitt aus `wiki/dsa-4.1/rituale/stabzauber.md` (Titel, Quelle, Text mit verschachtelten Listen, „↗ Obsidian“); der Ritualname ist ein Obsidian-Link (Anzeigetext = Bogen-Name, z. B. „Stabzauber: Fackel“, „Stabzauber: Stabverlängerung“). Badge (`Vol`) und Meta (Probe · AsP) stehen außerhalb des Links.
- **Layout gemessen (1280 und 400 px):** Details liegen in allen 10 Zeilen exakt auf der Textspalte (0,0 px Differenz); 137 Artikel-`li`, 0× `display:grid`, 0 Rahmen (Sprint-021-Falle bleibt geschlossen); kleinstes li/body-Verhältnis 0,97/0,92; längster Artikel Zauberspeicher 962 px @1280 (1829 px @400, Body 232 px); die 105 Zeichen lange Quelle bricht auf 2 Zeilen @1280 / 4 Zeilen @400 um, ohne Überlauf; `<summary>` 44 px @400; kein Seitenüberlauf.
- **Druck (Emulation, 794×1123):** Namenslink und Pfeil 14,62 : 1 (10/10 Ritual-, 15/15 SF-Links); alle Artikel-Elemente 14,62 : 1; `break-inside:auto` bei offenem Artikel; Body-Breite im 2-Spalten-Druck 316 px. Bildschirmfarbe des Links unverändert (`--ink`).
- **Parser/Kontext:** `held['rituale']['stabzauber'|'andere'][i]['wiki_path']` (voller Pfad inkl. `#Anker` oder `None`); `ctx['wiki_artikel']` enthält die 10 Ritual-Pfade (Schlüssel = voller `wiki_path`).
- **Tests:** synthetischer Held (`tests/heldfixtures.py::write_mini_held`) trägt jetzt auch Geld-Integration und den Kampf-Wund-Waffenfall; live gekoppelt bleiben bewusst genau zwei Tests (`test_build_context_wiki_artikel_smoke_live_vault`, `test_held_stabzauber_anchor_links_hit_a_section_of_the_wiki_article` — erfasst seit T1 automatisch auch `#Apport`).
- Alle Features aus Sprint 013–023 unverändert (Chronik Roh/Kompiliert/Register + Suche + Druck-Kopfzeile, Eigenschafts-Leiste, Inline-Werte, Zauberspeicher, Aussehen, Würfel, Steigern, Inventar, Sprachen, ZfW-Sortierung, Wund-Malus + Hausregel-Chips, Touch-Ziele @ 400 px, SF-/Zauber-Artikelvorschau, Desktop-Footer über dem Würfelpanel, Static-Render mit eingebettetem JS).

## Verifikation

- **Test Suite:** **586/586** bestanden, auch mit `-W error` (pytest-Option), beide Läufe nach Löschen aller `__pycache__` (Sprint-023-Stand 563: +6 T2, +5 T3, +7 T4, +4 T5, +1 Fix-Runde; Polish nur Kommentar). Mutationsproben je neuem Assert-Block (Reports unter `.superpowers/sdd/sprint-024/`).
- **Static Render:** `output/illaen-baernhold-dashboard.html` erfolgreich generiert (exit 0), **508 457 B** (Sprint 023: 481 641 B; Python-`read_text` normalisiert CRLF → 500 039 B); 0× `<script src>`, **50×** `class="artikel-details`; `git status` nach dem Render sauber. Golden-Vergleich (Baseline `fd6f593`): außerhalb der Karte „Stabzauber & Rituale“ byte-identisch, in der Karte nur 10 Namenslinks + 10 `<details>`; die Fix-Runde ändert nur 4 CSS-Zeilen.
- **Wiki:** `check-frontmatter.py` → 828 Dateien, 682 mit Frontmatter, 0 fehlerhaft; `wiki/` unberührt.
- **Reviews:** Task-Reviews T1–T5 (Sonnet) je Spec ✅ / Approved (0 Critical, 0 Important); Re-Review Fix-Runde (Sonnet) ADDRESSED; Gesamt-Review (Opus) **Ready to merge: Yes** (0 Critical, 0 Important, 7 Minor — 1 inline behoben, Rest deferred/getriaged); Escaping des neuen `href` geprüft (`obsidian_uri` quotet mit `quote(safe='/')`, wie die SF-Links, kein neues Loch). Berichte: `.superpowers/sdd/sprint-024/`.
- **Domänen-Grenze:** `git diff --numstat fd6f593 HEAD -- helden/illaen-baernhold abenteuer wiki` = genau `1 1 helden/illaen-baernhold/rituale.md`; `abenteuer/` und `wiki/` unberührt.
- **Browser-Runde:** durchgeführt (Ruling R9-Kontext: Fix-Umfang nur der Namenslink); echter Druckdialog und echter `PATCH`-Pfad weiter ungeprüft.

## Als nächstes (Sprint 025)

- **D-052** (Dashboard, S, ready) — Druck-Restposten der Zauber-Tab-Karten, alle vorbestehend und in der Sprint-024-Browser-Runde gemessen: `.meta` 1,95 : 1 und `.vol-badge` 4,12 : 1 der Stabzauber-Zeilen, `.speicher-box` 1,04 : 1 (seit Sprint 005), Druck-Überlauf `docScrollWidth` 881 gegen 779 (Ursache `wirkung-cell`/`mods-details` in der Karte „Spontane Modifikationen“, nicht die Ritual-Karte), blasser dotted-Unterstrich des Namenslinks im Druck; optional Grid-Stretch (SF-Karte wird bei offenen Ritual-Artikeln gleich hoch). Vorgehen wie D-046: Regeln im `@media print`-Block von `tabs.css` (Selektor-Falle: eigene Bildschirm-`color` erbt die Druckfarbe nicht), Print-Emulation über Static-`http.server` messen.
- **B-025** (tooling, S) — `test_inventar_model.py`: `test_geld_structure`/`test_geld_fallback`/`test_gesamt_kreuzer_math` prüfen die Test-Replik `_make_geld_dict`, nicht den Parser; entweder auf `load_held` (Mini-Held) umstellen oder die Replik streichen.
- **B-023** (wiki, M) — freistehende Buch-Sternchen (`ZfP*`, `LkP*`, `RkP*`, `TaP*`) werden von mistune zu `<em>` gepaart (113 Zeilen in 81 Dateien + 5 Zeilen Kursiv um `\*`). Trifft die Vorschauen erst, wenn betroffene Artikel eingebettet werden — `stabzauber.md` ist sauber escaped, `rituale-grundregeln.md` hat nur einen einzelnen Stern (Z. 35, keine Paarung); relevant, sobald weitere Bogen-Zeilen auf andere `rituale/*`-Artikel zeigen.
- **Kandidaten für neue EPICs (noch nicht im BACKLOG):** Footer-Einblenden glätten (`transition` auf `bottom`, aus Sprint 021); echter PATCH-Pfad im Browser (gegen eine Vault-Kopie); Zustände: optionale LE-/AU-Mali (vom User zweimal abgewählt — nicht ohne neue Anfrage).
- (Reihenfolge = Empfehlung, nicht Pflicht.)

## Bekannte Einschränkungen (bewusst ausgeklammert)

- **Identische, lange Quelle in allen 10 Ritual-Vorschauen:** `WdZ S. 105–115 (Aktivierungsregel S. 105, Apport S. 106, Stabzauber S. 107–111, Kristallkugel S. 111–115)` (105 Zeichen) — das ist die Artikelspanne, nicht die Seite des einzelnen Rituals; ` · ` trennt im Korpus legitime Zweitquellen, Kürzen wäre falsch. Umbruch gemessen (2/4 Zeilen), kein Defekt.
- **Ritual-Karte wächst bei vielen offenen Artikeln stark** (alle 10 offen ≈ 7 123 px @1280; Zauberspeicher allein 962 px); Grid-Stretch macht die SF-Karte daneben gleich hoch (viel Leerfläche). Beim Einzelöffnen unauffällig → in D-052 als optional geführt.
- **„Stabverlängerung“ = „Doppeltes Maß“ ist nur semantisch bestätigt** (Sprint-023-Ruling R10, unverändert): die Zeile im Bogen hat weiter leere Probe/AsP und Vol „?“; die Vorschau zeigt den Abschnitt „Doppeltes Maß“. Stimmt die Zuordnung im Spiel nicht, ist nur die Namenszelle in `rituale.md` zurückzusetzen.
- **Nicht eingebettet, obwohl im Wiki:** Flammenschwert und Schuppenhaut (Tabellen im Artikel) stehen nicht im Bogen — ihre Einbettung (Tabellen-Layout) wäre erst mit weiteren Bogen-Zeilen zu messen.
- **Bogen-Werte werden in den Ritualzeilen roh ausgegeben** (`r.name`, `r.effekt`, `r.vol` ohne `| e`, `autoescape=False`) — identisch zu den SF-/Zauber-Zeilen, vorbestehend; nur der neue `href` läuft durch `obsidian_uri` (`quote(safe='/')`). Der Bogen ist User-Domäne, kein fremder Input.
- **Deferred Minors des Sprints (Testhygiene, Gesamt-Review getriagt „bleibt deferred“):** `test_held.py` — `test_stabzauber_plain_name_has_wiki_path_none` deckt den Umlaut-Pfad nicht selbst ab, `andere`-Zeile mit Kopf „Effekt“ (Fallback) und Link ohne `#Anker` ungetestet; `test_rendering.py` — Parameter `{}`/leere Listen im `rituale`-Regressionstest ohne Defensiv-Nutzen (nur die `None`-Fälle tragen), `#Fehlt`-Warnung nicht per `caplog` belegt, `_ritual_card` dupliziert `_sf_card`, `'meta' not in link` als breiter Teilstring, `_ritual_li` trägt nur ohne `<ul>` im Fixture-Artikel, Klick-Handler-Test der Waffen-Fixture mit `data-at="\d+"` statt exakt 7/8, redundante `== 4`-Zählung und feste Untergrenze `len(cells) >= 3`.
- **Live-Kopplung am Bogen bleibt** bei genau zwei Tests (s. o.); `test_render_kampf_tab_*` mit `live_html` sind waffenzahl-unabhängig, aber bei 0 Waffen vakuös — der synthetische Waffenfall belegt die Nicht-Vakuität.
- **Übrige `.index`-Vorkommen in `test_rendering.py`** (~14, Struktur-/Reihenfolge-/JS-Text-Tests) bewusst unverändert (Ruling R4, B-024-Scope).
- **`_QUELLE_TEXT_RE` erkennt `> **Quelle: X**` nicht** (Doppelpunkt innerhalb des Fettbereichs → stilles `''`); im Korpus 0 Fälle. **`stabzauber.md`-Schlusszeilen:** Kraftfokus, Merkmalsfokus, Modifikationsfokus, Zauberspeicher enden mit `*Fazit:*` ohne Seitenangabe, Apport ohne Schlusszeile — die Vorschauen zeigen die Seite nur im Artikelkopf. **Buch-Mehrdeutigkeit Schuppenhaut** („je Spielrunde oder je RkP* würfeln“) im Artikel gekennzeichnet. **Zwei bewusst getrennte Pflegelisten** in `test_wikiartikel.py` (`ROHSTERN_DATEIEN`, `ROHSTERN_ZEILEN`): neue Fälle aus B-023 an beiden Stellen ergänzen.
- **`python -W error -m pytest` scheitert in dieser Umgebung schon beim Plugin-Import** (ipykernel-Warnung) — `-W error` als pytest-Option nutzen.
- **Zeilenenden** (autocrlf=true, jede Datei behält ihr Ende; Index durchgängig LF): CRLF im Working Tree = `held.py`, `rendering.py`, `zauber.j2`, `tabs.css`, `test_rendering.py`, `test_wikiartikel.py`, `output/*.html`; LF = `test_held.py`, `test_inventar_model.py`, `heldfixtures.py`, `parsers/wikiartikel.py`, Wiki-`.md`, Tracker, `rituale.md`, neue Sprint-Dokumente. Messen per `git ls-files --eol` oder Python-Bytezählung, nicht per `grep $'\r'`.
- **Nicht im echten Druckdialog / nicht unter `file://` / echter `PATCH`-Pfad ungeprüft** — unverändert aus Sprint 020/021; die Druck-Messungen sind Print-Emulation.
- **L24/L25 und Buchabgleiche zitieren gitignorierte Rohdateien** (`raw/pdf-extracted/wege-der-zauberei/kapitel-09-rituale.txt`); das spaltenrekonstruierte Transkript `.superpowers/sdd/sprint-023/wdz-stabzauber-transkript.txt` ist ebenfalls git-ignoriert (lokal erhalten).
- **Lehren (Process):** (a) **Der Browser findet Druck-Regressionen, die Tests und Reviews nicht sehen:** jede neu verlinkte/eingebettete Zeile auch in der **Print-Emulation** messen (der neue Link erbte die Druckfarbe von `.sf-name` nicht) — Fix-Runde per `SendMessage` an den Original-Implementierer, Nachmessung per `SendMessage` an den Browser-Agenten (Kontext intakt). (b) **Pre-flight am Code vor dem Dispatch** fand drei Plan-Fehler (Live-Smoke-Test wäre rot geworden; zwei Tests behaupteten „Anker ändert Dict nicht“; Apport-Effekt-Link darf nicht zu `wiki_path` werden), und der Plan nannte Tabellen (Flammenschwert/Schuppenhaut), die nicht eingebettet sind — die 10 Artikel haben 0 Tabellen (Korpus-Zählung, Ruling R8; Memory `feedback_verify_data_assumptions_on_corpus`). (c) Mutationsproben: **nie `git checkout --` zum Zurücksetzen** (ein Implementierer verlor damit ungespeicherte Arbeit, aus einer Kopie wiederhergestellt) — Kopie + Hash. (d) Review-Paket bei Commits mit `output/*.html` **von Hand ohne den Render-Diff** bauen (`git diff -- . ':!output'`); Golden-Vergleich per Python am Kartenmarker.
- **Process-Notizen:**
  - Briefs/Ledger/Reports von Hand unter `.superpowers/sdd/sprint-024/` (git-ignoriert, bleibt für die Verweise in `verification.md`/`plan.md` erhalten — bewusste Abweichung von „Workspace nach dem Review löschen“); Reviewer-Verdikt zuoberst, < 3500 Zeichen — kein Bericht abgeschnitten.
  - Alle Implementierer/Task-Reviews/Re-Review/Browser-Agent Sonnet, Gesamt-Review Opus; keine parallelen Implementierer; jeder Task-Commit per `git show --stat HEAD` gegen die erwartete Dateizahl geprüft; Controller-Gegenprobe (Testzahl + Render-Hash) nach jedem Task. Der Browser-Agent hielt die Nur-Lesen-Liste ein (nur Tab „Zauber“ + Summaries), Server-PID beendet, Port frei.
  - Kein Worktree, Commits direkt auf `master` (Ruling R1, CLAUDE.md-Workflow); kein Push.
- **Aus Sprint 017–023 unverändert offen:** Register hängt am Session-Format; `Verlauf`-Speichern in der Kompiliert-Ansicht nie im Browser angeklickt; inhaltliche Sichtung der vier Sessions (47 `(?)`-Stellen); Kommando-Mängel von `/session-compile`; Static-Render-Schreibaktionen liefern unter `file://` Fake-Erfolg; Chronik-Parser-Kleinigkeiten; `.journal-readonly`-Kontrast ~3,3:1; Sprint-013-Erbe (Kampf-Header-Wortlaut, `.talent-row`-Grid, Portrait-Plumbing); Tooltips auf Touch nicht sichtbar; Steigern-Negativfall @ 400 px nur bei 1280 px und im node-Test belegt; Desktop-Footer ≤ 480 px bleibt statisch; Footer springt beim Einblenden des Panels (Sprint 021); SF-Vorschau-Randfälle (`###`-/Blockreferenz-Anker, leere Abschnitte, doppelte Anker); Zustands-Chips = Hausregel, Wund-Regeln teilweise ausgeklammert (Wundschwellen, Zonenwunden, „Wunden ignorieren“); `js_function` (tests/jsfixtures.py) findet keine Funktion mit `)` im Default-Parameter.
