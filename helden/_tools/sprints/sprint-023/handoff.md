# Sprint 023 Handoff — Wiki-Genauigkeit, Quellen-Fallback & Test-Härtung (B-019…B-022)

## Fertig (alle Tasks abgeschlossen)

- ✅ T0: Sprint scaffold (`backlog.md` B-019…B-021 → In Progress, B-022 neu, plan.md) — `bd4f364`, Plan-Korrektur `78a9e1c`
- ✅ T1 (**B-019a**): `stabzauber.md` gegen WdZ neu aufgesetzt — 12 Rituale in 4 Gruppen (Meta/profan/arkan/esoterisch) inkl. **Apport**, Fehlwerte korrigiert (Doppeltes Maß Vol 1, Schuppenhaut MU/IN/CH +7, Bindung Vol 0, Hammer des Magus), **Fassungsvermögen 24/18/15/27** statt der erfundenen „Holzarten“-Tabelle, Kopfblöcke als Listen; `wiki-luecken.md` L24 ✅, L25(c) „Stabverlängerung“ = „Doppeltes Maß“ semantisch bestätigt — `8cd44de`
- ✅ T2 (**B-019b**): Detailregeln der 7 knappen Abschnitte, Flammenschwert-Misslingens-Tabelle (**L10**, im Buch 4 Zeilen 1–3/4/5/6), Schuppenhaut-Risiko (**L9**, Wurfrhythmus im Buch mehrdeutig → gekennzeichnet), Zauberspeicher-Komplexität, allgemeine Stabregeln S. 108; L9/L10 ✅ — `87119f9`
- ✅ T3 (**B-020**): `**… ZfP***` → `**… ZfP\***` in **10** Dateien / 39 Zeilen (Backlog nannte 3), 26 Tests gegen die echten Wiki-Dateien; Render-Diff = die 4 Odem-Arcanum-Zeilen — `8cd062f`
- ✅ T4 (**B-022**): Quelle-Fallback im Parser — `_quelle(fm, body)` liest `> **Quelle:**` aus dem Kopfblock (vor dem Anker-Schnitt), 23 Tests; Korpus 809 Artikel: 129 mit vorher leerer Quelle jetzt gefüllt, Render byte-identisch — `2556bd4`
- ✅ T5 (**B-021**): `WIKILINK_RE`-Anzeigetext verbietet `[[` (Korpus 3760 = 3760 Matches); Kampf-Wund-Hooks waffenzahl-unabhängig + `.index`-Härtung; `test_steigerbar.py` auf synthetischen Mini-Helden (`tests/heldfixtures.py`); Teil „Rest-`---`“ nicht reproduzierbar — `270bae4`, `939ca0e`, `75f9312`
- ✅ T6 (**L25(b)**): 9 Anker-Links in `helden/illaen-baernhold/rituale.md` (einmalige User-Freigabe; Anzeigetext wortgleich; `load_held(...)['rituale']` und Render byte-identisch) + 3 Tests — `4f289aa`
- ✅ Polish (Controller inline): 9 Minor-Funde der Task-Reviews — `7fa707e`; Final-Review-Fixes (L25 ✅, Quelle S. 105–115, Anführungszeichen) — `4698da1`
- ✅ T7: `verification.md`, `plan.md` (Task-Stände, Rulings R1–R13), Vault-`backlog.md` (B-019…B-022 → Done, B-023/B-024 neu); Wrap (dieses Dokument, `CLAUDE.md` → 23)
- Commits liegen **lokal** auf `master`: **nichts von Sprint 022 (ab T2) und Sprint 023 ist gepusht** (23 Commits vor `origin/master` vor dem Wrap-Commit).

## Was funktioniert

- **Wiki:** `stabzauber.md` ist buchgenau (WdZ S. 105–115) und **alle 12 `##`-Anker** sind per `load_wiki_artikel(…/rituale/stabzauber#<Name>)` ladbar — je ein Abschnitt, kein `<hr`, kein wörtliches `**`, Quelle `WdZ S. 105–115 (Aktivierungsregel S. 105, Apport S. 106, Stabzauber S. 107–111, Kristallkugel S. 111–115)`. Alle 682 Artikel mit Frontmatter sind gültiges YAML (`check-frontmatter.py` → 0 Fehler).
- **Heldenbogen `rituale.md`:** jede Stabzauber-Zeile trägt einen Anker-Link auf den passenden Wiki-Abschnitt; das Dashboard rendert unverändert.
- **Parser:** `parsers/wikiartikel.py` liefert `quelle` auch für frontmatter-lose Kapitelartikel (alle `rituale/*`), Frontmatter-`quelle` (+ `seite`) hat weiter Vorrang. `WIKILINK_RE` schluckt bei ungeschlossenem `[[` im Pfad **und** im Anzeigetext keinen echten Link mehr.
- **Tests:** `tests/heldfixtures.py` (`write_mini_held(tmp_path, rituale=…, zauber=…, …)`, `MINI_SLUG`) liefert einen synthetischen Helden; `test_steigerbar.py` liest den Live-Bogen nicht mehr. Live-gekoppelt bleiben bewusst zwei Tests (`test_build_context_wiki_artikel_smoke_live_vault`, `test_held_stabzauber_anchor_links_hit_a_section_of_the_wiki_article`) plus die B-024-Reste.
- Alle Features aus Sprint 013–022 unverändert (Chronik Roh/Kompiliert/Register + Suche + Druck-Kopfzeile, Eigenschafts-Leiste, Inline-Werte, Zauberspeicher, Aussehen, Würfel, Steigern, Inventar, Sprachen, ZfW-Sortierung, Wund-Malus + Hausregel-Chips, Touch-Ziele @ 400 px, SF-/Zauber-Artikelvorschau, Desktop-Footer über dem Würfelpanel, Static-Render mit eingebettetem JS).

## Verifikation

- **Test Suite:** **563/563** bestanden, auch mit `-W error` (pytest-Option), beide Läufe nach Löschen aller `__pycache__` (Sprint-022-Stand 464: +2 T1, +32 T2, +26 T3, +23 T4, +12 T5, +3 T6, +1 Polish). Mutationsproben je neuem Assert-Block (Reports unter `.superpowers/sdd/sprint-023/`).
- **Static Render:** `output/illaen-baernhold-dashboard.html` erfolgreich generiert (exit 0), **481 641 B** (Sprint 022: 481 589 B, +52 B); 0× `<script src>`, **40×** `class="artikel-details`; Diff zur Sprint-Start-Baseline: genau **8 Zeilen** = 4 Aufzählungszeilen im Odem-Arcanum-Vorschaublock (`**N ZfP***` → `<strong>N ZfP*</strong>`); `git status` nach dem Render sauber.
- **Wiki:** `check-frontmatter.py` → 828 Dateien, 682 mit Frontmatter, 0 fehlerhaft; kein unescaptes `**… ZfP***` mehr im Korpus.
- **Reviews:** Task-Reviews T1–T6 (Sonnet) je Spec ✅ / Approved (0 Critical, 0 Important); Gesamt-Review (Opus) „Ready to merge: With fixes“ (0 Critical, 1 Important = L25(b) noch „offen“, 3 Minor) — alle behoben. Buchtreue: T1/T2-Reviews prüften > 55 Werte, das Gesamt-Review alle 12 Kopfblöcke am Rohtext (0 Abweichungen). Berichte: `.superpowers/sdd/sprint-023/`.
- **Domänen-Grenze:** unter `helden/illaen-baernhold/` nur `rituale.md` (9 Namenszellen + 1 Halbsatz, freigegeben); `abenteuer/` unberührt.
- **Browser-Runde:** entfiel bewusst (Ruling R9) — kein Template-/CSS-/JS-Diff.

## Als nächstes (Sprint 024)

- **Dashboard-Backlog ist leer** (`BACKLOG.md`: In Progress und Backlog leer) — Sprint 024 braucht ein neues EPIC (per Brainstorming) oder Vault-Backlog-Arbeit.
- **D-051 Ritual-Artikelvorschau** (Dashboard, M–L; **noch kein EPIC angelegt**) — technisch voraussetzungsfrei startklar (L25 ✅): Anker-Links im Bogen, 12 ladbare Abschnitte, Quelle über T4. Anknüpfungspunkte: `parsers/held.py` ~Z. 391–406 (Stabzauber-Dicts tragen bisher **kein** `wiki_path` → `extract_wiki_path(row['Stabzauber'])` ergänzen, `name` bleibt `strip_wikilink`), `rendering.py` ~Z. 89 (`artikel_eintraege` um `held['rituale']['stabzauber']` erweitern), `templates/partials/zauber.j2` ~Z. 97 (Stabzauber-Schleife: Makro `partials/_artikel.j2::artikel_details` wie bei den SF), CSS `.sf-list > li` beachten (Sprint-021-Layoutfalle: Artikeltext darf nicht in die Zeilenregeln rutschen). Sinnvoll: eine Browser-Runde (Static über `http.server`), weil Tabellen (Flammenschwert/Schuppenhaut) und lange Kopfzeilen (s. u.) eingebettet werden.
- **B-023** (wiki, M) — freistehende Buch-Sternchen (`ZfP*`, `LkP*`, `RkP*`, `TaP*`) werden von mistune zu `<em>` gepaart (113 Zeilen in 81 Dateien + 5 Zeilen Kursiv um `\*`); Lösungsweg offen (Wiki-Massenedit vs. Parser-Vorbehandlung). Trifft die Vorschauen erst, wenn betroffene Artikel eingebettet werden — vor D-051 für `rituale/*` prüfen.
- **B-024** (tooling, S) — Restkopplung II: `test_inventar_model.py::test_load_held_geld_integration` (Live-Geldwerte), Waffenkarten-Fall der Kampf-Hooks nur live, rohe `.index` in `test_rendering.py` (~Z. 293–297, ~939).
- **Kandidaten für neue EPICs (noch nicht im BACKLOG):** Footer-Einblenden glätten (`transition` auf `bottom`, aus Sprint 021); echter PATCH-Pfad im Browser (gegen eine Vault-Kopie); Zustände: optionale LE-/AU-Mali (vom User zweimal abgewählt — nicht ohne neue Anfrage).
- (Reihenfolge = Empfehlung, nicht Pflicht.)

## Bekannte Einschränkungen (bewusst ausgeklammert)

- **„Stabverlängerung“ = „Doppeltes Maß“ ist nur semantisch bestätigt** (Ruling R10): „Stabverlängerung“ ist kein Buchname, das Doppelte Maß ist der einzige Stabzauber, der den Stab wachsen lässt (WdZ S. 108/109). Die Zeile im Bogen hat weiter leere Probe/AsP und Vol „?“ („bewusst offen, bis das im Spiel geklärt ist“); stimmt die Zuordnung im Spiel nicht, ist nur die Namenszelle in `rituale.md` zurückzusetzen.
- **Lange Vorschau-Kopfzeile ab D-051:** die Quelle der 12 Stabzauber-Anker ist `WdZ S. 105–115 (Apport S. 106, …)` (T4 nimmt die ganze Zeile; ` · ` trennt im Korpus legitime Zweitquellen, Kürzen wäre falsch); `.artikel-quelle` bricht um. Ähnlich: `kampf/kampfregeln.md` (`· **Thema:** …` würde als „· Thema: …“ erscheinen) und `grundregeln/zustaende.md` (353 Zeichen Seitenliste) — heute nicht eingebettet.
- **`_QUELLE_TEXT_RE` erkennt `> **Quelle: X**` nicht** (Doppelpunkt innerhalb des Fettbereichs → stilles `''`); im Korpus 0 Fälle.
- **`stabzauber.md`-Schlusszeilen:** Kraftfokus, Merkmalsfokus, Modifikationsfokus, Zauberspeicher enden mit `*Fazit:*` ohne Seitenangabe, Apport ohne Schlusszeile (der Quellenzeilen-Test schließt sie bewusst aus) — Anker-Vorschauen dieser fünf zeigen die Seite nur im Artikelkopf. Merkmalsfokus-Übersicht und L6 im Buchwortlaut („um einen Punkt erleichtert“), die frühere Auslegung „nicht auf ZfP*“ ist entfernt.
- **Buch-Mehrdeutigkeit Schuppenhaut:** „Für volle RkP* Spielrunden … würfeln Sie mit dem W20“ — ob je Spielrunde oder je RkP* gewürfelt wird, lässt der Buchtext offen; der Artikel gibt den Wortlaut wieder und kennzeichnet es.
- **Zwei bewusst getrennte Pflegelisten** in `test_wikiartikel.py` (`ROHSTERN_DATEIEN`, `ROHSTERN_ZEILEN`): neue Fälle aus B-023 an beiden Stellen ergänzen.
- **L24/L25 und die Buchabgleiche zitieren gitignorierte Rohdateien** (`raw/pdf-extracted/wege-der-zauberei/kapitel-09-rituale.txt`); das spaltenrekonstruierte Transkript `.superpowers/sdd/sprint-023/wdz-stabzauber-transkript.txt` ist ebenfalls git-ignoriert (lokal erhalten).
- **`python -W error -m pytest` scheitert in dieser Umgebung schon beim Plugin-Import** (ipykernel-Warnung, nicht unser Code) — `-W error` als pytest-Option nutzen (so schreibt es auch `/sprint-wrap` vor).
- **Zeilenenden** (autocrlf=true, jede Datei behält ihr Ende): CRLF = `held.py`, `test_rendering.py`, `test_wikiartikel.py`, `output/*.html`; LF = `parsers/wikiartikel.py`, `test_held.py`, `test_steigerbar.py`, `heldfixtures.py`, Wiki-`.md`, Tracker, `rituale.md`. Messen per `git ls-files --eol` oder Python-Bytezählung, nicht per `grep $'\r'`.
- **Nicht im echten Druckdialog / nicht unter `file://` / echter `PATCH`-Pfad ungeprüft** — unverändert aus Sprint 020/021.
- **Lehren (Process):** (a) Bash-Heredoc kollabiert `\\n` zu `\n` → echter Zeilenumbruch im Python-String (Collection-Fehler): Patchskripte mit Backslashes per Write-Werkzeug als Datei ablegen (Memory `feedback_tool_transport_control_chars`). (b) Backlog-/Plan-Behauptungen am Korpus messen, bevor sie den Brief prägen: „15 SF-Anker mit leerer Quelle“ waren 0, „3 Rohsternchen-Dateien“ waren 10, „pinnt 1 Waffenkarte“ war falsch (`kampf.j2` rendert nur die erste Waffe), Rest-`---`-Befund nicht reproduzierbar (Memory `feedback_verify_data_assumptions_on_corpus`). (c) Buch-Wiki-Tasks: ein spaltenrekonstruiertes Transkript als geteilte Grundlage + Reviewer, die Zahlen am Rohtext prüfen, fanden Fehler, die kein Testlauf sehen kann (erfundene Holzarten-Tabelle, falsch zugeordnete Regel).
- **Process-Notizen:**
  - Briefs/Ledger/Reports von Hand unter `.superpowers/sdd/sprint-023/` (git-ignoriert, bleibt für die Verweise in `verification.md`/`plan.md` erhalten); Reviewer-Verdikt zuoberst, < 3500 Zeichen — kein Bericht wurde abgeschnitten.
  - Implementierer T3 meldete `NEEDS_CONTEXT` bei Scope-Überschreitung (10 statt 4 Dateien) → Ruling per `SendMessage`; alle Implementierer committeten selbst, `git show --stat HEAD` je Task bestätigte die erwartete Dateizahl. Polish-Commit und Final-Review-Fixes inline vom Controller (Memory `feedback_quality_fix_inline`).
  - Alle Agenten Sonnet (Implementierer, Task-Reviews), Gesamt-Review Opus; keine parallelen Implementierer; kein Browser-/Server-Prozess gestartet.
- **Aus Sprint 017–022 unverändert offen:** Register hängt am Session-Format; `Verlauf`-Speichern in der Kompiliert-Ansicht nie im Browser angeklickt; inhaltliche Sichtung der vier Sessions (47 `(?)`-Stellen); Kommando-Mängel von `/session-compile`; Static-Render-Schreibaktionen liefern unter `file://` Fake-Erfolg; Chronik-Parser-Kleinigkeiten; `.journal-readonly`-Kontrast ~3,3:1; Sprint-013-Erbe (Kampf-Header-Wortlaut, `.talent-row`-Grid, Portrait-Plumbing); Tooltips auf Touch nicht sichtbar; Steigern-Negativfall @ 400 px nur bei 1280 px und im node-Test belegt; Desktop-Footer ≤ 480 px bleibt statisch; Footer springt beim Einblenden des Panels (Sprint 021); SF-Vorschau-Randfälle (`###`-/Blockreferenz-Anker, leere Abschnitte, doppelte Anker); Zustands-Chips = Hausregel, Wund-Regeln teilweise ausgeklammert (Wundschwellen, Zonenwunden, „Wunden ignorieren“); `js_function` (tests/jsfixtures.py) findet keine Funktion mit `)` im Default-Parameter.
