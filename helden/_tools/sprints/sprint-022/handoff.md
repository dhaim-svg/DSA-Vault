# Sprint 022 Handoff — Hygiene-Sprint: Regex-Härtung, Test-Fundament, Wiki-Vorbereitung, Repo-EOL (B-015…B-018)

## Fertig (alle Tasks abgeschlossen)

- ✅ T0: Sprint scaffold (`backlog.md` B-015…B-018 → In Progress, plan.md) — `cb4f966`
- ✅ T1: Push-Gate — 16 Commits (Sprint 020/021 + T0) nach `origin/master` gepusht (`f1bf38e..cb4f966`), damit ein Referenzpunkt vor der EOL-Änderung existiert
- ✅ T2 (**B-017**): `WIKILINK_RE` verbietet `[[` im Pfad (einzelnes `[` bleibt erlaubt, `[[b#X [y]|B]]` matcht weiter), 4 Tests; Korpus-Gegenprobe 3718 = 3718 Matches über 885 Dateien, einzige Abweichung: `sprints/sprint-021/handoff.md` (Prosa mit literalem `[[`) — `be267ca`
- ✅ T3 (**B-015**): `tests/jsfixtures.py` (`needs_node`, `js_function`/`js_function_body` über eine Klammerzählung, `run_node`) ersetzt 4 Kopien Funktionsextraktion + 6 node-Aufrufe in `test_rendering/register/wundregeln.py`; SF-/Artikel-Render-Tests laufen auf synthetischem Vault, **ein** Live-Smoke-Test bleibt (`test_build_context_wiki_artikel_smoke_live_vault`); kein Produktionscode-Diff — `59aa45e`, `b8e6f14`
- ✅ T4 (**B-018**): `wiki/dsa-4.1/rituale/stabzauber.md` mit `##`-Abschnitt je Stabzauber (11), die vier Foki-Blöcke wortgleich übernommen; Horriphobus `**N ZfP\***` (Vorschau zeigte Rohtext, jetzt `<strong>N ZfP*</strong>`, 8 Render-Zeilen); `wiki-luecken.md` **L24** (Tabelle weicht vom Buch ab, Apport fehlt, „13“ nicht belegt) + **L25** (Voraussetzungen der Ritual-Vorschau, Namensabgleich Held↔Wiki); 1 Test (`##`-Struktur + Anker-Ladbarkeit) — `7dcc824`, `b03810f`, Review-Fix `ac91060`
- ✅ T5 (**B-016**): `.gitattributes` `* text=auto`, ohne `--renormalize`; Index war schon durchgängig LF, `git status` blieb sauber — `64095e0`
- ✅ Polish: `test_jsfixtures.py` (7 Tests, Mutationsprobe je Assert), Wikilink-Test 3 beißt jetzt (`x [[a [[]] y`), Docstring-Vorbehalt für `)` in Parametern — `5e329e2`
- ✅ T6: Verifikation, `plan.md` (Task-Stände, Rulings R1–R10), `verification.md`, `backlog.md` (B-015…B-018 → Done, B-019…B-021 neu), Zählungs-Vorbehalt bei „13“ Stabzaubern — `3225ed1`; Wrap (dieses Dokument, `CLAUDE.md` → 22)
- Commits liegen **lokal** auf `master`: alles nach T1 (`cb4f966`) ist **nicht gepusht**.

## Was funktioniert

- **Wiki:** `stabzauber.md` ist per Anker ladbar — `load_wiki_artikel(…/rituale/stabzauber#<Name>)` liefert genau einen Abschnitt, ohne Warnung, ohne `<hr`. Alle 682 Artikel mit Frontmatter sind gültiges YAML (`check-frontmatter.py` → 0 Fehler).
- **Wikilinks:** ein ungeschlossenes `[[` im Wiki-Text schluckt keinen echten Link mehr (`held.py`, alle 5 Linkformen unverändert).
- **Test-Fundament:** die JS-Tests teilen `tests/jsfixtures.py`; die SF-/Artikel-Render-Tests sind vom echten Heldenbogen entkoppelt (löscht der User SF-Wikilinks im Bogen, werden sie nicht mehr rot).
- **Zauber-Tab Horriphobus:** Vorschau setzt die Reaktionsstufen jetzt fett statt mit Rohsternchen.
- Alle Features aus Sprint 013–021 unverändert (Chronik Roh/Kompiliert/Register + Suche + Druck-Kopfzeile, Eigenschafts-Leiste, Inline-Werte, Zauberspeicher, Aussehen, Würfel, Steigern, Inventar, Sprachen, ZfW-Sortierung, Wund-Malus + Hausregel-Chips, Touch-Ziele @ 400 px, SF-/Zauber-Artikelvorschau, Desktop-Footer über dem Würfelpanel, Static-Render mit eingebettetem JS).

## Verifikation

- **Test Suite:** **464/464** bestanden, auch mit `-W error`, beide Läufe nach Löschen aller `__pycache__` (Sprint-021-Stand 429: +4 T2, +24 T4 [parametrisierter `stabzauber.md`-Test], +7 Polish, T3 neutral). Mutationsproben je neuem Test/Helfer (alle rot, Dateien per Hash byte-genau zurück).
- **Static Render:** `output/illaen-baernhold-dashboard.html` erfolgreich generiert (exit 0), **481 589 B** (Sprint 021: 481 537 B, +52 B durch das Horriphobus-Escaping); 0× `<script src>`, **40×** `class="artikel-details` (25 Zauber + 15 SF); Diff zum Sprint-Start: genau 8 Zeilen im Horriphobus-Block.
- **Wiki:** `check-frontmatter.py` → 828 Dateien, 682 mit Frontmatter, 0 fehlerhaft.
- **Repo:** `git status` sauber; Steuerbytes 0x08/0x0C in allen Sprint-Dateien: keine; Index 984× `i/lf`, unverändert nach `.gitattributes`.
- **Reviews:** Task-Reviews T2–T4 (Sonnet) Spec ✅ / Approved (T4: 1 Important, behoben), Gesamt-Review (Opus) **Ready to merge: Yes** (0 Critical, 1 Important nicht blockierend → B-019, 4 Minor → B-021/Handoff). Rulings R1–R10 in `plan.md` („Ergebnis & Rulings“); vollständiger Bericht `.superpowers/sdd/sprint-022/final-review-full.md`.
- **Domänen-Grenze:** nichts unter `helden/illaen-baernhold/` oder `abenteuer/`.
- **Browser-Runde:** entfiel bewusst — kein UI-/Template-/CSS-/JS-Diff (einziger Render-Effekt: 8 Horriphobus-Zeilen, im HTML-Diff geprüft).

## Als nächstes (Sprint 023)

- **Dashboard-Backlog ist leer** (`BACKLOG.md`: In Progress und Backlog leer) — Sprint 023 braucht neue EPICs (per Brainstorming) oder Vault-Backlog-Arbeit (`backlog.md`: B-019, B-020, B-021).
- **B-019** (wiki, M) — `stabzauber.md` gegen WdZ S. 106–111 neu aufsetzen (L24): Apport ergänzen (12 Rituale, Gruppen Meta/profan/arkan/esoterisch), Doppeltes Maß / Schuppenhaut / Bindung des Stabes korrigieren (Fassungsvermögen statt „Vol je Holzart“), die 7 knappen Abschnitte auf Buchwerte + Detailregeln (L9/L10/L25), Frontmatter mit `quelle` ergänzen, „13“ auch in `rituale/_rituale.md`, Fettwert-Zeilen als Liste. **Behebt auch das Important des Gesamt-Reviews** (Abschnitte geben die als abweichend dokumentierten Werte mit Schlussvermerk weiter). Voraussetzung für die Ritual-Vorschau.
- **B-020** (wiki, S) — `**…ZfP***` in `zauber/odem-arcanum.md` (Z. 35–38), `alchimie/alchimie-grundregeln.md` (Z. 123), `alchimie/artefakt-herstellung.md` (Z. 89) escapen (Muster wie Horriphobus); danach Render-Diff prüfen, falls der Artikel im Bogen verlinkt ist.
- **B-021** (tooling, S) — Test-Härtung: `test_render_kampf_tab_has_wund_stat_hooks_for_wound_stats` pinnt genau 1 Waffenkarte im echten Bogen (rot, sobald der User eine zweite Waffe einträgt), `test_steigerbar.py` 7× `load_held` live, `test_rendering.py` (~Z. 583) hängt an der Reihenfolge Zauberliste→SF-Karte, `test_wikiartikel.py` (~Z. 868) lässt ein Rest-`---` als letzte Zeile durch, `WIKILINK_RE`-Anzeigetext-Gruppe akzeptiert weiterhin `[[`.
- **Ritual-Artikelvorschau** (Dashboard-EPIC, M–L) — erst nach B-019 **und** nach dem User-Schritt aus L25(b): `helden/illaen-baernhold/rituale.md` verlinkt `[[…/stabzauber]]` ohne Anker; jede Stabzauber-Zeile bräuchte `[[wiki/dsa-4.1/rituale/stabzauber#<Wiki-Name>\|<Anzeige>]]` (Held „Fackel“ = Wiki „Ewige Flamme“, „Stabverlängerung“ vermutlich „Doppeltes Maß“, unbestätigt). Kein Eingriff durch die Bibliothekarin ohne Anfrage.
- **Kandidaten für neue EPICs (noch nicht im BACKLOG):** Footer-Einblenden glätten (`transition` auf `bottom`, aus Sprint 021); echter PATCH-Pfad im Browser (gegen eine Vault-Kopie); Zustände: optionale LE-/AU-Mali (vom User zweimal abgewählt — nicht ohne neue Anfrage).
- (Reihenfolge = Empfehlung, nicht Pflicht.)

## Bekannte Einschränkungen (bewusst ausgeklammert)

- **`stabzauber.md` enthält bekannte Fehlwerte:** gegen WdZ geprüft weichen ab — Doppeltes Maß (Effekt „+3 auf FF, +2 auf KL“, Vol „—“; Buch: Stab wächst auf doppelte Länge, Vol 1), Schuppenhaut (Erschaffungsprobe „MU/IN/KO“; Buch: MU/IN/CH +7), Bindung des Stabes (Vol „1 pAsP“; Buch: 0), „Vol-Angaben 24/18/15/27“ ist das Stab-Fassungsvermögen, Apport fehlt, „13“ nicht belegt. Übersichtstabelle bewusst nicht umgebaut (L24, eigener Wiki-Auftrag); die drei betroffenen Abschnitte vermerken es in der Schlusszeile („weicht laut L24 vom Buch ab“). Solange nichts eine Ritual-Vorschau rendert, ohne Wirkung.
- **Anker-Vorschauen von `stabzauber.md` hätten eine leere Quelle** (kein Frontmatter → kein `quelle`), und die Fettwert-Zeilen der 7 knappen Abschnitte sowie der Foki-Blöcke rendern in mistune als **ein** Absatz — beides Teil von B-019/der Ritual-Vorschau.
- **L24/L25 zitieren eine gitignorierte Rohdatei** (`raw/pdf-extracted/wege-der-zauberei/kapitel-09-rituale.txt`): der Buchabgleich ist aus dem Repo allein nicht reproduzierbar; Seitenzählung nach den Fußzeilen der Textdatei.
- **Noch live gekoppelte Tests** (B-021): `test_render_kampf_tab_has_wund_stat_hooks…`, `test_steigerbar.py`, die Live-Tests, die nur die Existenz des Helden brauchen (Tab-Container, Kampagnen-Slug, Register, `LE: { current:`-Hoisting; Liste in `.superpowers/sdd/sprint-022/task-3-report.md`), plus der bewusste Smoke-Test.
- **`js_function` findet keine Funktion mit `)` im Default-Parameter** (`[^)]*`) und zählt Klammern naiv (kein `{`/`}` in Strings/Kommentaren) — im Docstring vermerkt, kein aktueller Aufrufer betroffen. `from tests.jsfixtures import …` (in `test_wundregeln.py`) braucht `tests/__init__.py` und den Standard-Importmodus (`--import-mode=importlib` bräche es).
- **Zeilenenden-Falle bei `core.autocrlf=true`:** 918 getrackte Textdateien sind im Working Tree reines LF, 65 CRLF, 1 gemischt. Der nächste Checkout/Reset/`stash pop` schreibt die LF-Dateien nach CRLF um; `git status` bleibt sauber (Index LF), es gibt keine getrackten Shell-Skripte, nichts bricht. `.gitattributes` `text=auto` ändert daran nichts (Zustand vorher wie nachher). Jede Datei behält ihr vorgefundenes Ende (Python/`output/*.html` CRLF; Wiki-`.md`, Tracker, neue Dateien LF).
- **Nicht im echten Druckdialog / nicht unter `file://` / echter `PATCH`-Pfad ungeprüft** — unverändert aus Sprint 020/021.
- **Lehre (Process):** (a) `grep -c $'\r'` meldet in dieser Shell **jede** Zeile als CR — Zeilenenden per Python-Bytezählung oder `git ls-files --eol` messen (Memory `feedback_tool_transport_control_chars`); die CRLF-Angaben in drei Briefs waren deshalb falsch und wurden erst durch `git ls-files --eol` entdeckt. (b) Backlog-Formulierungen prüfen, bevor sie den Brief prägen: „fast identische Runner“ waren drei verschiedene Skripte, „Render bleibt unverändert“ war falsch (Horriphobus im Bogen) — Rulings im Ledger (Memory `feedback_verify_data_assumptions_on_corpus`). (c) Ein Reviewer, der einen Bericht abgeschnitten liefert, liefert den Rest auf `SendMessage` nach (klappte im T3-Review).
- **Process-Notizen:**
  - Briefs/Ledger/Reports von Hand unter `.superpowers/sdd/sprint-022/` (git-ignoriert, bleibt für die Verweise in `verification.md`/`plan.md` erhalten; `sdd-workspace`/`task-brief` kollidieren mit dem Basename `plan`); Reviewer-Verdikt zuoberst, < 3500 Zeichen.
  - Der T2-Implementierer committete nicht (Controller übernahm nach Prüfung des Diffs); T3/T4-Implementierer committeten selbst, `git show --stat` je Task bestätigte die erwartete Dateizahl. T5, die T4-Review-Fixes und der Polish-Commit wurden vom Controller inline gemacht (Memory `feedback_quality_fix_inline`) und vom Gesamt-Review mitgeprüft.
  - Alle Agenten waren Sonnet (Implementierer, Task-Reviews), Gesamt-Review Opus; keine parallelen Implementierer; kein Browser-/Server-Prozess gestartet.
- **Aus Sprint 017–021 unverändert offen:** Register hängt am Session-Format; `Verlauf`-Speichern in der Kompiliert-Ansicht nie im Browser angeklickt; inhaltliche Sichtung der vier Sessions (47 `(?)`-Stellen); Kommando-Mängel von `/session-compile`; Static-Render-Schreibaktionen liefern unter `file://` Fake-Erfolg; Chronik-Parser-Kleinigkeiten; `.journal-readonly`-Kontrast ~3,3:1; Sprint-013-Erbe (Kampf-Header-Wortlaut, `.talent-row`-Grid, Portrait-Plumbing); Tooltips auf Touch nicht sichtbar; Steigern-Negativfall @ 400 px nur bei 1280 px und im node-Test belegt; Desktop-Footer ≤ 480 px bleibt statisch; Footer springt beim Einblenden des Panels (Sprint 021); SF-Vorschau-Randfälle (`###`-/Blockreferenz-Anker, leere Abschnitte, doppelte Anker); Zustands-Chips = Hausregel, Wund-Regeln teilweise ausgeklammert (Wundschwellen, Zonenwunden, „Wunden ignorieren“).
