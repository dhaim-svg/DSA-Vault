# Sprint 019 Handoff — Wund-/Zustände-Audit + Zauber-Feinschliff (D-041, D-046, D-044)

## Fertig (alle Tasks abgeschlossen)

- ✅ T0: Sprint scaffold (BACKLOG.md D-041/D-044/D-046 → in-progress, plan.md) — `ab8a5d0`
- ✅ T1 (**D-041a**): Regelrecherche in WdS/WdH/WdE + neuer Wiki-Artikel `grundregeln/zustaende.md` (Wunden belegt, Zustände „andere Mechanik"/„nicht belegt", optionale LE-/AU-Mali), `_grundregeln.md`, `wiki-luecken.md` L23; zwei Wiki-Bestandsfehler korrigiert (`eigenschaften.md`/`proben.md`: „SP ≥ Wundschwelle" → „>", WdS S. 57) — `3cb043e`, `d872643`, `8603466`
- ✅ T2 (**D-041b**): Wundlogik regelkonform — neue reine `static/wundregeln.js` (`window.DSAWundregeln`: `JE_WUNDE`, `wundMod`, `geltungText`), `session.js` mit Effekt-Modell (`{label, wunden, mods}` / `{label, alle, mod}`) und `DSASession.{computeActiveEffects, probeMod, statMod, attrMod}`, `dice.js` `getWundMod(cfg)` nach Probenart (`schaden` → 0), Basiswert-Overlay `base→eff` im Kampf-Tab (`data-wund-stat`/`data-wund-base`), erste JS-Verhaltenstests (`tests/test_wundregeln.py`, node) — `ebb702a`
- ✅ T3 (**D-041c**): Zustands-Chips als Hausregel gekennzeichnet (Flag, Regelwerk-Hinweis im Tooltip, Klasse `hausregel`, Badge „(Hausregel)"), Legende unter den Chips, Wiki-Tabelle „Dashboard-Wert heute" angeglichen — `89fd7ef`, `0aa7ef3`; Test-Reparatur `da46ea0`
- ✅ T4 (**D-046**): Zauberliste — Druck-Farben für acht Selektoren (+ Kopfzeile/Modifikations-Details), Grid-Minima `160/240/38/80/90/200` (Summe 878 px), toter `.merk`-Selektor entfernt — `37fee44`, `1300ffc`, `1dec437`
- ✅ T5 (**D-044**): Banner ≤ 600 px, Footer ≤ 480 px statisch/44 px, Steigern-Scrollbereich (`div.steiger-scroll`), Inventar-Modifier-Klassen, Artikel-Summary 44 px — `ce26e50`, `9304983`
- ✅ T6: Verifikation (Suite, Static-Render, zwei Playwright-Runden, Gesamt-Review Opus + Fix-Runde + scoped Re-Review) — Final-Review-Fix `1970b15` (GE-Abzug pro Attribut in Talent-/Zauberproben, Legenden-Split, Test-Härtung), Static-Render `e749739`, `b98c547`, plan.md/verification.md `b1ecdab`
- Commits liegen **lokal** auf `master`, **nichts gepusht** (`origin/master` steht weiter bei `0351829`, 41+ Commits davor, Sprint 018 + 019).

## Was funktioniert

- **Wunden (Kampf-Tab, Widget `+`/`−`):** pro Wunde AT/PA/FK/INI-Basis und GE −2, GS −1 (WdS S. 57), kumulativ. Overlay `base→eff` auf den Basiswerten (AT/PA/FK/INI/GS, Waffen-AT/PA) und auf `GE`-Spans; Würfelpanel: AT/PA −2 je Wunde, Einzel-GE-Probe −2 je Wunde, **Talent-/Zauberproben nutzen den reduzierten GE-Wert** (`parseProbe` + `attrMod`, Panel-Modifikator bleibt 0), MU/KL/IN/CH/FF/KO/KK, MR, SO, Schaden (TP) unberührt. Widget-Text und Badge nennen den Geltungsbereich („AT/PA/FK/INI/GE −4, GS −2" bzw. „Wunden ×2: GE −4").
- **Zustands-Chips:** fünf localStorage-Schalter, Werte unverändert (−2/−2/−4/−2/−2, wirken auf alle Proben über `dp-mod`, nicht auf Basiswert-Anzeigen, nie auf Schaden), sichtbar als Hausregel markiert; Legende unter den Chips (Wund-Satz bleibt im Static-Render sichtbar, der Zustände-Halbsatz nur wenn Chips gerendert werden).
- **Wiki:** `grundregeln/zustaende.md` (Regelstatus je Zustand mit Buch/Seite, Ableitung GE-Abzug in Proben, optionale LE-/AU-Mali).
- **Zauber-Tab:** Druck: alle Textfarben dunkel auf Papier (14,62:1), Kopfzeile und Modifikations-Details eingeschlossen; Desktop-Grid ohne Überstand ab 1071 px; Kompaktlayout ≤ 1070 px mit 44-px-Summary „▸ Artikel"; Artikelvorschau und ZfW-Sortierung aus Sprint 018 unverändert.
- **Mobile 400 px:** alle 8 Tabs ohne Überlauf (`scrollWidth == clientWidth == 385`); Banner einspaltig (Titel `clamp(26px, 8.5vw, 44px)`); statischer Footer mit 44-px-Controls; Steigern-Tabellen im fokussierbaren Scrollbereich mit Scroll-Hinweis.
- Alle Features aus Sprint 013–018 unverändert (Chronik Roh/Kompiliert/Register, Eigenschafts-Leiste, Inline-Werte, Zauberspeicher, Aussehen, Würfel, Steigern, Inventar, Sprachen, Static-Render mit eingebettetem JS).

## Verifikation

- **Test Suite:** 379/379 bestanden, auch mit `-W error` (Baseline 333 → +46: `test_wundregeln.py` neu mit 25 [node-Verhaltenstests für Regeltabelle, `getWundMod`, `parseProbe`, `attrMod`, echte `session.js` im Fake-DOM, Struktur-Scans], `test_rendering.py` +21 [49 → 70: Hooks, Legende, CSS-Scans, Grid-Arithmetik, Footer/Banner/Steigern/Inventar]). Jeder neue Test hat eine belegte Negativprobe gegen den Vorzustand.
- **Static Render:** `output/illaen-baernhold-dashboard.html` erfolgreich generiert (exit 0), identisch zum committeten Stand; 446 203 B (Sprint 018: 434 594 B, +11,6 KB durch `wundregeln.js`, CSS, Hooks, Legende); 0× `<script src>`, 7× `data-wund-stat`, 25× `artikel-details`, 0× `computeWundPenalty`.
- **Browser (Playwright, Static über `http.server`, nur lesend, `innerWidth` je Messung verifiziert; Details `verification.md`):** Runde 1 **36 PASS / 0 FAIL / 1 nicht messbar**, Nachprüfung A8 **19 PASS / 0 FAIL**. Gemessen bei 2 Wunden: nur `GE 13→9` (14 Spans mit Pfeil), INI 10→6, GS 8→6, AT 7→3, PA 8→4, FK 8→4, Waffe 13→9/9→5; `#dp-mod`: GE/AT/PA −4, MU/KK/Talent/Zauber/Schaden 0; GE-Wert in Talent-/Zauberpanel 13→9; mit Schmerz-Chip AT/GE −6, MU/Talent/Zauber −2, Basiswerte unverändert, Einzel-GE ohne Doppelabzug. Grid-Überstand 0 px bei 1071/1100/1130/1280 (181 Kinder, zu und offen). Konsole nur `favicon.ico` 404, `/api/…/mtime` 404, `PATCH` 501 (erwartet).
- **Domänen-Grenze:** `git diff --name-only 832b6a6..HEAD` berührt nur `helden/_tools/`, `output/`, `wiki/dsa-4.1/grundregeln/` (T1-Dateien), `wiki-luecken.md`; nichts unter `abenteuer/`, `helden/illaen-baernhold/`, `raw/`.
- **Gesamt-Review (Opus):** 0 Critical; Important 2 (Wikilinks bare vs. relativ → widerlegt, Bare-Namen sind Vault-Konvention; GE-Abzug in Talent-/Zauberproben → behoben); Minor 7 (2 behoben, Rest unten).

## Als nächstes (Sprint 020)

- **D-045** (Chronik-Druck, S) — steht an der Backlog-Spitze: Ansichten-Konsistenz und Register-Filter im Druck.
- **D-047** (Touch-Ziele & Mobile-Restposten bei 400 px, S) — neu aus dem Browser-Check zu D-044: Inventar-Eingaben 30 px, Zustands-Chips 25 px, `.codex`-Padding bei offenem Würfelpanel ungemessen, Scroll-Hinweis nur bei Überlauf, `.zfw-num` im Druck scopen.
- **D-048** (Zustands-Chips: doppelte Darstellung + Regelbasis, S) — **braucht eine User-Entscheidung** vor der Planung: Overlay oder Panel-Vorbelegung, und ob die erfundenen Chip-Werte durch die belegten optionalen Regeln (LE-/AU-Mali WdS S. 57/83, Schmerz-Probe S. 82) ersetzt werden sollen.
- **B-013** (Vault-`backlog.md`, wiki, M) — Frontmatter der Zauberartikel reparieren (103/268 ungültiges YAML, `wiki-luecken.md` L22), danach Fallback-Parser in `parsers/wikiartikel.py` entfernen.
- (Reihenfolge = Empfehlung, nicht Pflicht; BACKLOG.md-Reihenfolge ist die Priorität.)

## Bekannte Einschränkungen (bewusst ausgeklammert)

- **Zustands-Chips = Hausregel:** In WdS/WdH/WdE (extrahierte Bücher) gibt es keinen festen Probenmalus für Schmerz/Furcht/Betäubung/Verwirrung/Erschöpfung; Wege der Zauberei und Zoo-Botanica wurden **nicht** geprüft (`wiki-luecken.md` L23). Die Chips erscheinen weiter doppelt (Overlay + Panel-Vorbelegung, vorbestehend) → D-048.
- **Optionale LE-/AU-Probenmali** (WdS S. 57/83) sind im Wiki dokumentiert, im Dashboard **nicht** umgesetzt (vom User abgewählt). Die **Wundschwellen-Berechnung** (WS = KO/2, 1/2/3 Wunden nach SP, Eisern/Glasknochen) ist nicht im Dashboard — es zählt Wunden, der Spieler trägt sie ein. Zonenwunden/Trefferlokalisierung (WdS S. 107) und „Wunden ignorieren" (S. 82) nicht abgebildet.
- **`file://`-Static-Render:** `session.js` läuft nur bei http(s) → keine Overlays/Chips; Panel-Vorbelegung und GE-Attributwert in Talent-/Zauberproben funktionieren über den `DSAWundregeln`-Fallback aus `[data-wunden]` (Wert kommt aus der Frontmatter beim Rendern). Direkt unter `file://` weiterhin nicht im Browser geprüft (Playwright blockt `file:`).
- **Entscheidungsnotiz GE-Abzug:** Die gewählte User-Option lautete „Talent-/Zauberproben bleiben unmodifiziert"; umgesetzt ist „kein pauschaler Malus, aber der GE-Wert in der Probe ist reduziert" (Overlay, Wiki-Artikel und Panel stimmen so überein). Rückbau: `wundAttrMod` in `dice.js` und `attrMod` in `session.js` entfernen.
- **Bekannte Minors aus den Reviews:** Badge-/Ternary-Regexe in `test_wundregeln.py` formatierungsabhängig (scheitern laut, nicht still); `test_session_has_no_stale_verify_rules_comment` scannt ganz `session.js` nach „TODO"; Banner-Test rechnet ein Modell (0,83 em/Zeichen), gemessen wurde die echte Breite im Browser; Tooltips sind auf Touch nicht sichtbar (Legende + Badge sind der einzige Hausregel-Hinweis); je Steigern-Tabelle 1 zusätzlicher Tab-Stop; führendes Leerzeichen im Wund-Satz der Legende ohne Chips (unsichtbar).
- **Node-Tests** (`test_wundregeln.py`) laufen nur mit installiertem `node` (sonst `skip`); im Repo gibt es weiterhin keinen JS-Runner.
- **Process-Notizen:** Briefs/Ledger von Hand unter `.superpowers/sdd/sprint-019/` (git-ignoriert; `sdd-workspace`/`task-brief` kollidieren mit dem Basename `plan`); **Steuerzeichen-Falle:** Regex-Escapes für Wortgrenzen (und Form-Feed) werden bei der Tool-Übergabe gelegentlich zu 0x08/0x0C — ein Test war dadurch still vakuös (Sprint 019 gefunden/repariert); nach jedem Task alle geänderten Dateien auf diese Bytes scannen (Memory `feedback_tool_transport_control_chars`). Browser-Verifikation läuft gegen das Static-Render über `python -m http.server` (nur lesend); `.playwright-mcp/` entsteht bei Läufen und wird gelöscht (`.gitignore`-Zeile weiterhin offen); `.claude/settings.json` bleibt untracked (User entscheidet).
- **Aus Sprint 017/018 unverändert offen:** Register hängt am Session-Format; Register-Filter bleibt im Druck (D-045); `Verlauf`-Speichern in der Kompiliert-Ansicht nie im Browser angeklickt; inhaltliche Sichtung der vier Sessions (47 `(?)`-Stellen); Kommando-Mängel von `/session-compile`; Static-Render-Schreibaktionen liefern unter `file://` Fake-Erfolg; Chronik-Parser-Kleinigkeiten; `.journal-readonly`-Kontrast ~3,3:1; Sprint-013-Erbe (Kampf-Header-Wortlaut, `.talent-row`-Grid, Portrait-Plumbing, Python/JS-Würfel-Mathe-Mirror ohne Drift-Check — die Wundregel hat jetzt eine einzige JS-Quelle, der Python-Mirror kennt Wunden weiterhin nicht); Vorschau nur für Zauber (Rituale/SF ohne).
