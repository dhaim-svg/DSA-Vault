# Dashboard Backlog — Illaen Dashboard

> EPIC-Tracker für Dashboard-Entwicklung. D-NNN-IDs.
> Format: state = ready | in-progress | done | blocked

## In Progress

_(keine)_

## Backlog

| EPIC | Title | Effort | State | Blocked by |
|------|-------|--------|-------|------------|
| D-065 | **`POST /api/commit` hat keinen CSRF-Schutz.** `api_commit()` liest den Body per `request.get_json(silent=True)` (toleriert auch Nicht-JSON-Bodies) und prüft keinen Origin/Referer/Token — ein Cross-Origin-Formular-POST (kein Preflight nötig) kann einen echten `helden/`-Commit auf dem laufenden Server auslösen. Die Antwort bleibt Cross-Origin unlesbar (keine CORS-Header), Impact daher ein ungewollter, aber reversibler lokaler Commit (kein Datenverlust, `git reset`/`revert` möglich). Vorbestehend, nicht durch D-064 eingeführt — von der Sprint-032-Gesamtreview beim D-064-Review gefunden. | S | ready | — |
| D-066 | **Verschluckte Fehler (D-063s `FileNotFoundError` in `api_held`, D-064s verworfener Git-Stderr in `api_commit`) landen aktuell nirgends im Server-Log — bewusste „Kein Logging-Ausbau"-Entscheidung pro Einzel-Fix, akkumuliert sich aber sprintübergreifend.** Ein einziges `app.logger.warning(...)` (oder äquivalent), das die gesamte bisher bekannte verschluckte-Fehler-Oberfläche auf einmal abdeckt, statt die Logging-Frage bei jedem neuen Pfad-Leak-Fix erneut aufzuschieben. Von der Sprint-032-Gesamtreview vorgeschlagen (Minor, kein Blocker für D-064). | S | ready | — |

_(Vault-`backlog.md`: weiterhin leer, kein B-Task in Sprint 029/030/031/032.)_

### Gestrichen

| EPIC | Title | Gestrichen am | Grund |
|------|-------|----------------|-------|
| D-033 | Quick-Capture in Footer-Bar | 19.09.2026 | Kein Live-Schreibpfad mehr (D-030-Fallback: Drive bleibt Quelle) |
| D-034 | Ereignis-Auto-Log | 19.09.2026 | Dito — hätte auf demselben Live-Schreibpfad wie D-033 aufgebaut |

Details siehe Beschreibung unten (D-033/D-034 wurden nicht einfach vergessen — bewusst gestrichen, s. D-030).

### Beschreibungen

**D-033 / D-034 — Quick-Capture & Ereignis-Auto-Log — *entfallen 19.09.2026***
Beide Live-Editing-Features setzten eine synchron beschreibbare Chronik-Datei im Vault voraus. Da Drive Quelle bleibt (D-030-Fallback, Junction-Ansatz gescheitert), gibt es keinen Live-Schreibpfad mehr, in den das Dashboard schreiben könnte, ohne beim nächsten Import überschrieben zu werden. Ersatzlos gestrichen — der User schreibt weiterhin direkt in Google Drive, keine Dashboard-Interaktion während des Spiels vorgesehen.

## Done

| EPIC | Title | Effort | Sprint |
|------|-------|--------|--------|
| D-064 | **`api_commit`s Fehlerpfad leakt keinen rohen Git-Stderr-Text mehr in der JSON-Antwort.** `server.py:149`s `jsonify({'ok': False, 'error': result.get('error', ...)}), 500` ersetzt durch eine feste generische Fehlermeldung (`'git operation failed'`) — Fix ausschließlich an der HTTP-Grenze, exaktes D-063-Präzedenzmuster: `git_ops.commit_helden()`s Rückgabe-Kontrakt (roher Stderr in `error`) bleibt intern unverändert, Status bleibt 500 (kein Client-Fall wie bei D-063s 404). Neuer Test erzwingt einen echten Git-Fehler (stale `.git/index.lock`) und prüft die Dual-Assertion (`str(tmp_path) not in body` + Windows-Laufwerksbuchstaben-Regex). Task-Review (Sonnet): Spec ✅, 0 Findings, Approved. **Sprint-032-Gesamtreview (Opus) reproduzierte Leak + Fix unabhängig in einer Scratch-Kopie** (nie den Projekt-Tree berührt) und maß die Dual-Assertion empirisch nach: gegen den zurückgesetzten Code besteht `str(tmp_path) not in body` vakuos (git nutzt Forward-Slashes), nur die Laufwerksbuchstaben-Regex diskriminiert korrekt — bestätigt die D-063-Lehre erneut. Verifizierte zusätzlich unabhängig den Scope-Entscheid (`api_patch_value`/`api_patch_kampagne` nicht betroffen — komplette Fehler-Vokabular von `held_writer.patch()` durchsucht, keine absoluten Pfade darin). 688/688 bestanden, auch mit `-W error` nach `__pycache__`-Löschen. 0 Critical/Important, 3 Minor: 1 (Test-Kommentar ohne Begründung) per Controller-Polish behoben, 2 als neue EPICs gefiled (**D-065** CSRF auf `/api/commit`, **D-066** konsolidiertes Logging für D-063+D-064s verschluckte Fehler). 687 → 688 Tests | S | 032 |
| D-063 | **`api_held`s Exception-Handler leakt keine absoluten Dateisystempfade mehr in der Fehler-JSON.** Bare `except Exception as exc: return jsonify({'error': str(exc)}), 500` (`server.py:78-79`) ersetzt durch `except FileNotFoundError: return jsonify({'error': 'not found'}), 404` — exakt das Muster von `api_etag` im selben File. Kein neuer Blanket-Catch: jede andere Exception faellt jetzt auf Flasks eigenen, sicheren 500-Handler durch (`debug=False`, kein Traceback/Pfad im Body). Vorbestehender Fund (nicht durch D-062 eingefuehrt), von der Sprint-030-Gesamtreview beim Scratch-Worktree-Test gegen den ungefixten `eeb8736`-Stand entdeckt. Task-Review (Sonnet): Spec ✅, 1 Minor (toter `mkdir()`-Testsetup). **Sprint-031-Gesamtreview (Opus) reproduzierte Leak + Fix unabhaengig empirisch** (Scratch-Kopie beider Commits via `git archive`, Produktions-Semantik `debug=False`): `c42d46e` (vor Fix) leakte real den vollen Windows-Pfad im 500-Body, `8e8aa05` (nach Fix) liefert sauber 404 ohne Pfad; eine erzwungene `ValueError` faellt nach dem Fix auf Flasks generische 500-Antwort durch (Message fehlt im Body) — die „debug=False macht das sicher"-Annahme empirisch bestaetigt. Lief den neuen Testfile selbst gegen den ungefixten Stand: Testfaelle (a) und (c) schlagen dort fehl (= diskriminieren korrekt), (b) besteht beidseitig (echte Regression). Volle Suite selbst nachgelaufen (687 bestanden). Fand zusaetzlich einen verwandten, aber Scope-fremden Leak (`api_commit`/`git_ops.py`, rohe Git-Stderr-Message) → als **D-064** neu gefiled statt mitgefixt. 2 der 3 Gesamtreview-Minors per trivialem Controller-Polish behoben (Kommentar + toter Code, kein Subagent). 684 → 687 Tests | S | 031 |
| D-062 | **`slug_param` in den `/api/held/<slug_param>/...`-Routen ist jetzt gegen Path-Traversal geprueft — ebenso die Seiten-Route `/held/<path:s>`.** Neuer `_valid_slug(s)`-Helper (`re.fullmatch(r'[a-z0-9_-]+', s or '')`, identisches Zeichenklassen-Muster wie der bestehende Campaign-Guard) als erste Zeile in allen 5 betroffenen Routen (server.py) — `held_page` → `404`, `api_held`/`api_mtime`/`api_etag`/`api_patch_value` → `400 {'error':'invalid slug'}`. Route-Ebene bewusst als einziger Guard-Ort (Ruling): anders als beim `campaign`-Feld (D-061) gibt es fuer `slug`/`s` keinen zweiten, HTTP-erreichbaren Eintrittspunkt (kein JSON-Body-Bypass moeglich) — ein Funktions-Level-Guard in `_resolve_base`/`load_held` waere unbenutzte Redundanz. Entdeckt bei D-061 (Sprint-029-Planung), dort bewusst nicht mitbehoben. **Sprint-029-Gesamtreview bestaetigte empirisch: `PATCH /api/held/../value` -> HTTP 200, echtes Ueberschreiben einer `.md`-Datei im Vault-Root.** **Sprint-030-Gesamtreview mass zusaetzlich gegen den ungefixten Code (Scratch-Worktree bei `eeb8736`): Werkzeugs Routing blockt nur `/` (`%2F`), NICHT `\` (`%5C`) — `..%5Csub`/`..%5C..` ueberschrieben real, teils **ausserhalb des Vault-Root**, nicht nur `.md` direkt im Vault-Root wie urspruenglich angenommen.** Fix (Allowlist-Regex statt `/`-spezifischem Denylist) deckt auch diesen Vektor ab, gegen den gefixten Stand verifiziert. Lehre (Handoff): „Werkzeugs Routing schliesst Pfad-Separatoren aus" ist auf Windows falsch — diese Annahme nirgends sonst wiederverwenden. Task-Review fand 1 Important (Diff-Snapshot vor einer Controller-Ruling-Ergaenzung), per Scoped-Re-Review ADDRESSED bestaetigt. Gesamtreview (Opus): „Ready to merge" — reproduzierte Exploit + Fix unabhaengig in einem Scratch-Worktree, kopierte den neuen Testfile auf den ungefixten Stand (11 von 13 neuen Tests schlagen dort fehl = diskriminieren korrekt, 0 vakuos vs. 5/8 in Sprint 029), Mutationsprobe bestaetigt. 1 vorbestehender Pfad-Leak-Fund (nicht durch diesen Fix eingefuehrt) als **D-063** neu gefiled. 671 → 684 Tests | S | 030 |
| D-061 | **PATCH-Routen validieren jetzt sowohl das `file`- als auch das `campaign`-Locator-Feld gegen Path-Traversal.** Neuer `_safe_join(base, rel_file)`-Helper in `writers/held_writer.py` (löst auf, prüft `relative_to(base)`, fängt Null-Byte/OS-Fehler ab, lehnt Verzeichnisse als `file`-Wert ab) sichert `patch()` und `etag_for()`; `_resolve_base()` validiert `campaign` jetzt per `re.fullmatch(r'[a-z0-9_-]+', ...)` — schließt damit auch den Bypass über `/api/held/<slug>/value`s ungefilterten JSON-Body (der Regex-Guard existierte bisher nur auf Routen-Ebene für `/api/kampagne/<camp>/value`). Sprint-029-Planung erweiterte den Backlog-Titel bewusst um das `campaign`-Feld (gleiche Fehlerklasse). Task-Review fand einen Null-Byte-Crash-Regressionsfund (`.resolve()` unabgefangen) → 1 Fix-Runde. Gesamtreview (Opus) verifizierte den Produktionsfix unabhängig durch Revert + Re-Test in einer Scratch-Kopie (bestätigte reale Exploitierbarkeit aller 4 Vektoren pre-fix inkl. echtem Datei-Überschreiben) und fand, dass 5 von 8 neuen Tests auch gegen den ungefixten Code bestanden hätten (vakuos — Opfer-Fixtures ohne erreichbaren Inhalt) → 1 Fixwelle machte alle Tests diskriminierend (echte Opfer-Inhalte + Assertion auf die konkrete Fehlermeldung statt nur `ok is False`), plus ein gebündelter Minor-Fix (Verzeichnis als `file`-Wert crashte vorher unbehandelt). Entdeckte, aber bewusst nicht mitgefixte verwandte Lücke (anderes Feld, `slug_param`) als **D-062** neu gefiled — Gesamtreview bestätigte sie als HTTP-erreichbar mit echtem Überschreiben, nicht nur theoretisch. 662 → 671 Tests | S | 029 |
| D-060 | **Footer gleitet jetzt synchron mit dem Würfelpanel ein/aus statt zu springen** (Lücke seit Sprint 021 gemessen: 331→197→108→28px, seither unverändert offen). `transition:bottom 0.2s ease` auf `#footer-bar` (base.css), synchron zur `.dice-panel`-Transition (0,2s ease, tabs.css:348-353). Test verifiziert die Synchronität relational — liest `.dice-panel`s reale Transitions-Dauer per Regex und vergleicht, statt zwei Werte hart zu kodieren (Gesamt-Review-Fund: die ursprüngliche Testversion hätte eine künftige Dauer-Änderung des Panels stillschweigend nicht mehr gefangen), Mutationsprobe bestätigt den Fang. Eigene Task-Review fand zuerst einen Critical: Report behauptete „volle Test-Suite bestanden", lief aber nur `test_rendering.py` (140 von 662) — Fix-Runde mit echtem Voll-Lauf bestätigte 662. 661 → 662 Tests | S | 028 |
| D-059 | **PATCH-Pfad „Verlauf speichern" hat jetzt automatisierte Testabdeckung für Routen-Wiring, den `camp`-Guard und JSON-Body-Handling — NICHT für das `file`-Feld selbst.** Neue `tests/test_server.py` (5 Tests, Muster `test_commit.py:107-131`): Erfolgsfall mit On-Disk-Verifikation, `camp`-Regex-Guard (3 parametrisierte Verletzungen), Malformed-JSON→400 — durchgehend gegen `tmp_path`-Fixture-`VAULT_ROOT`, echter Vault nie berührt (User-Entscheidung Sprint-028-Planung: kein Live-Browser-Klick diesen Sprint, keine User-Domäne-Freigabe nötig). Ungeprüft blieb bewusst das `file`/Locator-Feld (`held_writer.py:76-77`, `target = base / rel_file` ohne Pfad-Traversal-Check) — Gesamt-Review-Fund: Commit-Nachricht „abgesichert" hatte den Umfang überzeichnet, Gap jetzt als **D-061** nachgetragen statt implizit mitgelesen. 656 → 661 Tests | S | 028 |
| D-058 | **Probe-Spalte im Zauber-Tab-Druck jetzt einzeilig** (war seit Sprint 025 in 25/25 Zeilen zweizeilig) **+ Ritual-/SF-Karten-Grid-Stretch im Zauber-Tab behoben.** `.spell`-Grid-fr-Anteile verschoben (Probe 1,4→1,9fr, Kosten 1→0,7fr, Wirkung 2,2→2fr, Summe unverändert 6,4fr) — `scrollWidth` @703/718/615px identisch vor/nach (662/677/574px), keine Überlauf-Regression gegenüber der in D-052/D-053 vermessenen Grenze. Neue `.grid.align-top{align-items:start}`-Modifier-Klasse nur an der Zauber-Tab-Grid-Instanz (zauber.j2:91) behebt den Stretch (SF-Karte blieb bei 1626,9px statt mit 10 offenen Ritual-Vorschauen auf 7122,6px zu wachsen); die 3 anderen `.cols-2`-Stellen (profil/inventar/kampf) unangetastet und per Test gepinnt (User-Entscheidung Sprint-028-Planung: kein globaler `.cols-2`-Umbau). **Nebenwirkung von der Gesamt-Review gefunden, nicht von T2 gemessen:** die schmalere Kosten-Spalte lässt 13 von 25 Zeilen im Druck stärker umbrechen — Summe der 25 Zeilenhöhen 1733,70→1810,95px (**+77,25px/+4,5 %**), 6 Zeilen tatsächlich höher, größter Einzelzuwachs +24,49px; auch die knapp schmalere Wirkung-Spalte wrapt bei 3 Zeilen unerwartet mehr. Kein Überlauf, kein Rollback nötig — Kernziel (einzeilige Probe) bleibt erreicht, Nebenwirkung dokumentiert statt verschwiegen (echter Seitenumbruch-Effekt ungeprüft, s. Bekannte Einschränkungen im Handoff). 653 → 656 Tests | S–M | 028 |
| D-057 | **`#tab-profil table *`-Flächenschlag entschärft — kein aktiver Bug (nur 1 Tabelle existierte im Tab), aber Regressionsrisiko für künftige Tabellen beseitigt** (2× seit Sprint 026 verschoben, „Regressionsrisiko"). Sessions-Tabelle (profil.j2:82) bekam eigene Klasse `.sessions-table`, `tabs.css:168` zielt darauf statt pauschal auf `table`/`table *`. Reines Refactoring — Golden-Render-Diff (`render_dashboard()` bei `253876f` vs. `5eca7d5`, gehasht + gedifft) zeigte exakt 3 erwartete Zeilen Differenz (Selector-Text, Kommentar, `class`-Attribut), sonst byte-identisch. Brauchte 2 Fix-Runden bis zur echten Evidenz: Runde 1 diffte trotz Anforderung weiterhin nur Quelltext statt gerenderten Outputs. 652 → 653 Tests | S | 028 |
| D-055 | **Der Verlaufstext druckt vollständig statt zu 80–89 % abgeschnitten.** v1 (`beforeprint` zieht `.journal-verlauf` auf `scrollHeight`) maß im **Bildschirm**-Layout (1280px-Fenster: clientWidth 1081px), die echte Druckbreite ist aber viel schmaler (~584px) — derselbe Text bricht dort auf mehr Zeilen um. Die eigene v1-Verifikation nutzte durchgängig 718px, wo Druck- und Bildschirmbreite zufällig zusammenfallen, und versteckte den Fehler damit strukturell. Die Gesamt-Review maß eigenständig bei zwei Fensterbreiten (1280px vs. 718px) und fand **48 Zeilen Differenz (~17 % weiterhin abgeschnitten)**. Fix: v1 komplett ersetzt durch ein `<pre class="journal-verlauf-print">`-Mirror, bei `beforeprint` per `textContent = t.value` aus dem Live-Wert befüllt (kein serverseitig veralteter Text — Staleness-Vorgabe bleibt erfüllt) und vom Druck-Layout selbst korrekt umbrochen, unabhängig von der Spaltenbreite. Nachgemessen bei 1280px **und** 718px: vollständiger Text aller 4 Sessions. **Methodenbefund:** `beforeprint`-Messungen im Bildschirm-Layout sind bei druckbreitenabhängigen Werten (Zeilenumbruch) nicht übertragbar — Breiten-Verifikation braucht künftig mindestens zwei unterschiedliche Fensterbreiten. 638 → 645 Tests (v1), nach B-027 dazwischen (645→648) dann 648 → 652 (Fix-Runde) | S–M | 027 |
| D-054 | **Die LeP/AsP/AuP-Zahlen sind im Ausdruck sichtbar.** `.vital::after` konnte nie funktionieren (`attr()` liest kein Attribut eines Nachfahren; `data-current` wurde projektweit nirgends gesetzt). Ruling: das reale `.vital-input`-Feld bleibt im Druck sichtbar (kein JS-Sync, der Browser druckt den aktuellen Formularwert ohnehin live), entkleidet als Text; `.vital-max`/`.vital-sep` bekommen eigene Druckfarbe statt im ausgeblendeten `.vital-stepper` unterzugehen. Fix-Runde: eine vorbestehende, nie geprüfte `opacity:0.7` verdünnte den Kontrast der beiden letzteren auf **6,20:1** statt der zunächst behaupteten 14,62:1 — korrigiert (`opacity:1 !important`, Vorbild `tabs.css:91/95`), alle drei Werte jetzt **14,62:1**, verifiziert per Print-Emulation **und** echtem `page.pdf()`+`pdftoppm`-Pfad. 633 → 638 Tests | S–M | 027 |
| D-056 | **Die getönte Papieroptik erscheint im echten Ausdruck weiß — Ruling: Weiß bleibt Zielzustand.** Ohne `print-color-adjust:exact` (projektweit nirgends gesetzt) druckt Chromium Hintergründe bei Standardeinstellungen nicht — belegt per `page.pdf()` + `pdftoppm` in Sprint 026, von Implementierer und Reviewer unabhängig reproduziert. **Kein Defekt:** gegen echtes Weiß ist jeder verwendete Druck-Kontrastwert besser als gegen die getönte Papierfarbe (`--paper-ink` 18,52 statt 14,62 : 1, `--paper-rule` 4,32 statt 3,41 : 1). In der Sprint-027-Planung entschieden: `print-color-adjust:exact` **nicht** setzen, keine Kontrast-Neubewertung gegen `#ece4d0` — reine Dokumentationsentscheidung ohne Code-Änderung | S | 027 |
| D-053 | **Druck-*Kontrast* der übrigen 7 Tabs — nicht die Vollständigkeit des Ausdrucks.** Gemessen statt geschätzt: **37 Gruppen / 495 Elemente** von 1503 Textknoten (steigern 10, inventar 10, profil 9, sprachen 6, talente 3, kampf 1) — der Backlog hatte 3 Werte genannt; **Überlauf 0** bei 794/718/615 px, die D-052-Grid-Ursache war zauber-spezifisch. Nachgemessen: **0 Restgruppen**, Chronik in allen 3 Ansichten 0, Zauber-Tab 0/500 unverändert (Entpräfixierung von `.card-title .meta` ohne Regression), **Bildschirm 0 Abweichungen** (3840 Knoten je Viewport). Über den Zuschnitt hinaus gefixt: Pfeil-Bindung ↗, `.sg-erf` entkleidet statt ausgeblendet (sonst wäre die gewählte Erfahrungsstufe im Ausdruck verschwunden), `.journal-verlauf` 1,07 → 18,52 : 1, `.sg-cart` (verdeckte im **echten PDF** eine Tabellenzeile — in der Emulation unsichtbar), drei datenabhängige Badges/Platzhalter, sowie die Chronik-Ansicht „Kompiliert" mit 3 von der Erstmessung nie erfassten Gruppen. Neuer generischer Wächter gegen künftige `sticky`/`fixed`-Elemente im Druck. **Methodenbefund:** Print-Emulation ≠ echter Druck (Chromium druckt Hintergründe ohne `print-color-adjust:exact` nicht) — ohne Folgen für die Fixes, aber die Verifikation braucht künftig den PDF-Pfad. **Offen geblieben:** fehlende Vitalwerte (D-054), Verlaufstext zu 80–89 % abgeschnitten (D-055), getönte Papieroptik druckt weiß (D-056). 615 → 633 Tests | S–M | 026 |
| D-052 | Druck des Zauber-Tabs: **Vermessung vor dem Fix (T2) widerlegte die Backlog-Zuordnung** — der Überlauf (`docScrollWidth` 881 gegen 779) kam aus dem `.spell`-Grid der Zauberliste (Spaltenminima 160/240/38/80/90/200 = 858 px inkl. Gaps gegen 733 px Innenbreite, im echten Druck ≈ 703 px Seitenbreite), nicht aus „Spontane Modifikationen“; statt der drei bekannten Kontrastwerte fanden sich **20 Gruppen 1,04–4,28 : 1** (`.sf-name .meta` maß 4,28 statt 1,95, die 1,04 stammten von `.slot-zauber`, nicht vom Kasten) → alle auf `paper-ink` 14,62 : 1; Druck-Grid mit `minmax(0,Xfr)` (überlaufsicher: `scrollWidth` 779/703/600 px bei 794/718/615 px Viewport), Slot-Schaltflächen im Druck ausgeblendet, Namenslink-Unterstrich 1,13 → 14,62 : 1, Vol-Badge/Speicherkasten als Kästen mit sichtbarem Rand; **nur Zauber-Tab — die übrigen 7 Tabs sind nicht vermessen (→ D-053)**; Bildschirm unverändert (400 Werte verglichen, 0 Abweichungen); Grid-Stretch der Ritual-/SF-Karte nicht angefasst; kosmetisch: Probe bricht in 25/25 Zeilen zweizeilig, der Pfeil ↗ steht @ 703 px bei 5 von 25 Namen allein in der Folgezeile | S | 025 |
| D-051 | Ritual-Artikelvorschau: Stabzauber- (9) und Apport-Zeile im Zauber-Tab zeigen ihren `##`-Abschnitt aus `stabzauber.md` als `<details>` (Makro `partials/_artikel.j2`, geteilt mit SF/Zauber); Parser liefert `wiki_path` je Ritual-Zeile (nur Namensspalte), `build_context` bettet die 10 Artikel ein, Apport-Namenszelle im Bogen als Anker-Link (einmalige User-Freigabe); Browser-Fund: Namenslink im Druck 1,07 : 1 (eigene Bildschirm-`color` erbt die Druckfarbe von `.sf-name` nicht) → `tabs.css` 14,62 : 1, bessert auch die 15 SF-Links; Static-Render 481 641 → 508 457 B, 40 → 50 Vorschauen, längster Artikel 962 px @1280 | M | 024 |
| D-050 | Artikelvorschau für Sonderfertigkeiten: 15 von 17 SF-Zeilen zeigen nur ihren `##`-Abschnitt (`load_wiki_artikel`, Anker → `split_sections`, Warnung bei fehlendem/leerem Abschnitt), `<details>`-Block als Makro `partials/_artikel.j2` geteilt mit Zauber, `WIKILINK_RE` erlaubt einzelnes `]` (Merkmalskenntnis-Anker); Browser-Fund: SF-Zeilenregeln trafen auch `li` im Artikeltext → `.sf-list > li` + Invariante (längster Artikel 7 841 → 808 px); Static-Render 450 → 481 KB, 40 Vorschauen | M | 021 |
| D-049 | Desktop-Footer über dem offenen Würfelpanel: `#footer-bar{bottom:calc(28px + var(--dice-panel-h,0px))}` (Panel z-index 200 verdeckte die Leiste, −277 px @ 1280 → Abstand 28 px); ≤ 480 px unberührt | S | 021 |
| D-048 | Zustands-Chips wirken nur noch über die Panel-Vorbelegung: `applyWundModsToProben` nutzt `attrMod` statt `probeMod` (kein Attribut-Overlay mehr auf 203 `[data-attr]`-Spans, Wund-Overlay bleibt), Legende präzisiert; Chip-Werte bleiben Hausregel (User-Entscheidung 19.09.2026) | S | 020 |
| D-047 | Touch-Ziele & Mobile-Restposten @ 400 px: Inventar-Eingaben/„+ Hinzufügen“ (30 px) und Zustands-Chips (25 px) → 44 px nur ≤ 480 px; offenes Würfelpanel reserviert per `--dice-panel-h` (`dice.js` + `body`-Padding, Lücke Footer↔Panel +15,8 px); Steigern-Scroll-Hinweis/`tabindex` nur bei echtem Überlauf (`ResizeObserver`); Druck-Selektor `.spell .zfw-num` gescopt | S | 020 |
| D-045 | Chronik-Druck: nur die aktive Ansicht (Roh/Kompiliert/Register) wird gedruckt, Register-Sonderregel entfällt; aktiver Register-Filter als Druck-Kopfzeile „Gefiltert nach: „…“ — n/N Einträge“ (`textContent`); mit Print-Emulation im Browser gemessen | S | 020 |
| D-046 | Zauberliste: Druck-Kontrast (Name-Link, ZfW, ZD, Kosten, Wirkung, Submeta, Kopfzeile, Modifikations-Details) 1,1:1 → 14,62:1 gemessen; Grid-Minima gesenkt (Summe 978 → 878 px), Überstand 0 px bei 1071/1100/1130/1280; toter `.merk`-Selektor entfernt | S | 019 |
| D-044 | Mobile/Touch: Banner ≤ 600 px einspaltig (Titel `clamp`), Footer ≤ 480 px statisch + 44 px, Steigern-Scrollbereich als `div.steiger-scroll` (fokussierbar, `role=region`, Scroll-Hinweis), Inventar-Modifier-Klassen, Artikel-Summary 44 px im Kompaktlayout | S | 019 |
| D-041 | Wund-/Zustände-Audit: Wund-Malus regelkonform (WdS S. 57: AT/PA/FK/INI/GE −2, GS −1 je Wunde; TP/Talent/Zauber unberührt) über neue `static/wundregeln.js` + `DSASession.{probeMod,statMod,attrMod}`, GE-Abzug pro Attribut auch in Talent-/Zauberproben, Basiswert-Overlay im Kampf-Tab; Zustands-Chips als Hausregel gekennzeichnet + Legende; Wiki-Artikel `grundregeln/zustaende.md`, `wiki-luecken.md` L23; erste JS-Verhaltenstests (`test_wundregeln.py`, node) | M | 019 |
| D-043 | Zauber: ZfW-Sortierung im Kompaktlayout erreichbar — Toolbar-Button (Standard → ZfW ↓ → ↑, Tastatur/ARIA, ≥ 44 px), `static/zauber-sort.js` statt Inline-Skript, Zeilen-Wrapper `[data-spell-list]` (Legende bleibt hinter der Liste) | S | 018 |
| D-018 | Zauber: Artikelvorschau als `<details>` in der Zauberzeile — 25 Artikel zur Render-Zeit eingebettet (`parsers/wikiartikel.py`, mistune `escape=True`, Wikilinks → `obsidian://`), Fallback-Parser für 103/268 Artikel mit ungültigem Frontmatter-YAML; Static-Render 379 → 434 KB | L | 018 |
| D-042 | Chronik-Parser erkennt `Datum: 13. Phex -> Start` als IG-Datum (`13. Phex (Start)`), 5 Tests | S | 017 |
| D-040 | Mobile 400 px: Überlauf in Zauber/Steigern/Inventar/Profil beseitigt — alle 8 Tabs ≤ 400 px, Zauber-Kompaktlayout ≤ 1070 px, Footer-Leiste umbricht | S | 017 |
| D-036 | NSC-/Orts-Register: `parsers/register.py`, 3. Ansicht „Register" im Chronik-Tab (55 Einträge), clientseitige Suche, zur Render-Zeit generiert | M | 017 |
| D-039 | Static-Render interaktiv: JS wird eingebettet (`JS_FILES`/`js_files()`/`inline_js`), Hinweis-Banner `#static-hinweis`, README — Tabs/Würfel unter `file://` | S | 016 |
| D-038 | Bug: `session.js` lief nie (`IS_SERVED`-Kollision) → IIFE + Top-Level-Kollisionstest, Browser-Gegenprüfung 9/9 | S | 016 |
| D-035 | `/session-compile`-Kommando + 4 Spielabende kompiliert (Session 1–4), Platzhalter-Session entfernt | M | 016 |
| D-037 | dashboard.html.j2 in Partials + eigenes CSS zerlegen (CSS → static/*.css, zur Render-Zeit eingebettet; 8 Tab-Partials; geteilter Render-Kontext) | L | 015 |
| D-032 | Chronik-Tab (📜, Roh/Kompiliert-Umschalter, read-only Roh-Ansicht, /chronik-bild-Route, Parser-Fix Bild+Text) | M | 015 |
| D-031 | parsers/chronik.py (Spielabend/IG-Tag/Szenen-Parser, verifiziert gegen echte Chronik) | M | 014 |
| D-030 | Chronik-Import-Skript (Drive → Vault, einseitig — Junction-Ansatz verworfen, s. Sprint-014-Handoff) | S | 014 |
| D-029 | Aussehen vervollständigen (Gewicht-Feld, offene Felder visuell markiert, Portrait-Plumbing) | S | 013 |
| D-028 | Erschaffungsprobe + AsP je Stabzauber (Nachschlage-Info) | S | 013 |
| D-027 | Zauberspeicher-Auslöseprobe (MU/IN/KL, würfelbar, +1 Erschwernis/Slot) | M | 013 |
| D-026 | Inline-Eigenschaftswerte vervollständigen (Kampftechniken, Wundabzug; `.mod-probe` bewusst ausgenommen — Modifikator-Formeln, keine Attribut-Proben) | M | 013 |
| D-025 | Eigenschafts-Leiste über Talente-/Zauber-Tab (sticky, mit Wund-/Zustandsbadge) | M | 013 |
| D-024 | Stabzauber: allgemeine Aktivierungsregel als Hinweiszeile | S | 012 |
| D-023 | Talente/Zauber: Eigenschaftswerte inline + Probe prominenter | M | 012 |
| D-022 | Profil: Aussehen & Kleidung als strukturierte Sektion | M | 012 |
| D-021 | Steigern: Session-Erfahrungs-Modifikator (SKT-Spaltenverschiebung) | M | 011 |
| D-020 | Steigern: Steigerungsspalte in eigene Tabelle (steiger-table) | M | 010 |
| D-019 | Steigern: Experiment-/Auswahl-Modus (Warenkorb) | L | 010 |
| D-017 | Sprachen: Komplexitäts-Spalte ausrichten | S | 009 |
| D-016 | Profil: Schlechte Eigenschaften / Prinzipientreue ohne Truncation | S | 009 |
| D-015 | Inventar: Formatierung & abgeschnittener Text | S | 009 |
| D-014 | Footer-Layout: Session/Sichern-Überlappung + Commit-Feld gruppieren | S | 009 |
| D-013 | Vitalia-Stepper in einer Reihe | S | 009 |
| D-012 | Würfel-Panel Lesbarkeit (tab-übergreifend) | S | 009 |
| D-011 | showIndicator-Dedup | S | 008 |
| D-010 | Commit-Message-Input | S | 008 |
| D-009 | Stufen-Aufstieg | M | 007 |
| D-008 | Sprachen mit Komplexitäts-Grenze | S | 007 |
| D-007 | Session-Commit-Button | S | 006 |
| D-006 | Session-Notizen → Journal | M | 006 |
| D-005 | Zauberspeicher im Stab | M | 005 |
| D-004 | Inventar / Geld / Verbrauch | M | 004 |
| D-003 | Tab-Navigation + AP & Steigerung | L | 003 |
| D-001 | Flask-Server + Vitalia Write-back + Wunden + Zustände | L | 001 |
| D-002 | Würfelintegration (3W20 Proben, Schaden, Panel) | L | 002 |
